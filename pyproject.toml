[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bristol_ml"
version = "0.0.0"
description = "Reference ML/DS architecture with a GB day-ahead electricity demand forecaster as the worked example."
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = { text = "MIT" }
authors = [{ name = "Sam Drew", email = "comm@sdrew.com" }]
dependencies = [
    "hydra-core>=1.3,<2",
    "omegaconf>=2.3,<3",
    "pydantic>=2.7,<3",
    "httpx>=0.27,<1",
    "tenacity>=9,<10",
    "pyarrow>=16,<22",
    "pandas>=2.2,<3",
    "loguru>=0.7,<1",
]

[dependency-groups]
dev = [
    "pytest>=8,<9",
    "ruff>=0.6,<1",
    "pre-commit>=3.7,<5",
    "pytest-recording>=0.13,<1",
    "vcrpy>=6,<8",
    "jupyter>=1,<2",
    "nbconvert>=7,<8",
    "matplotlib>=3.8,<4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/bristol_ml"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["bristol_ml", "conf"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
addopts = "-ra --strict-markers --record-mode=none"
filterwarnings = [
    "ignore::DeprecationWarning:vcr.*",
]
