# Architecture

The canonical architecture description is [`DESIGN.md` §3](../DESIGN.md#3-system-architecture). This file exists so `docs/` has a discoverable entry point — do not duplicate content here. When the architecture changes, update `DESIGN.md` §3 and record the change in `CHANGELOG.md`.
