# Architecture

The canonical architecture description is [`DESIGN.md` §3](../intent/DESIGN.md#3-system-architecture). This directory exists so `docs/` has a discoverable home for structural design work — do not duplicate the overview here. When the architecture changes, update `DESIGN.md` §3 and record the change in `CHANGELOG.md`.

- [`decisions/`](./decisions/) — MADR-format architecture decision records. Append-only: supersede older ADRs with new ones rather than editing.
