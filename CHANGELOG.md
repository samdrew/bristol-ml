# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Fixed

- Stage 8 `ScipyParametricModel`: bounded TRF solver replaces unconstrained
  Levenberg-Marquardt, eliminating the rank-deficient-fold divergence that
  produced cross-fold mean MAE ~167 600 MW on the nb08 30-day-window splitter
  (plan 08a).  Physically-motivated bounds (`alpha ∈ [0, 100 000]`;
  `beta_heat`, `beta_cool ∈ [0, 5 000]`; Fourier coefficients
  `∈ [−50 000, 50 000]`) are applied across both the `loss="linear"` and
  robust-loss branches.  Three `pcov`-unreliability overrides (bound
  saturation, implausibly large Moore-Penrose variance, zero-information
  Jacobian column) surface unidentifiable parameters as `std_err = inf`.
  AC-3 verified: 1-year window mean MAE 4 897 MW.  AC-4 verified: 30-day
  catastrophic-window mean MAE 4 563 MW (down from ~167 600).

- `bristol_ml.registry.list_runs` now skips run directories whose
  artefact is the pre-Stage-12 `model.joblib` file (with no
  `model.skops` sibling), emitting one `loguru.warning` per skipped run
  that names the `run_id` and directory.  Such a run cannot be loaded
  — `registry.load` raises `RuntimeError` for it under the Stage 12
  D10 skops-only contract — so surfacing it on the leaderboard misled
  callers that iterate `list_runs(...)` followed by `load(...)` (the
  Stage 11 ablation notebook hit this in production after the Stage 12
  migration).  Operator action remains unchanged: re-fit to migrate to
  skops, or delete the legacy run directory.  Regression guard:
  `tests/unit/registry/test_registry_list_describe.py::test_registry_list_skips_legacy_joblib_run`.
- `NaiveModel.save` and `NnTemporalModel.save` now normalise the
  saved `DatetimeIndex` to nanosecond precision via
  `pd.DatetimeIndex.as_unit("ns")` before extracting the int64 vector.
  Pandas 2.x propagates parquet's default microsecond unit into
  `DatetimeIndex.unit`, so a frame loaded from the Stage 3 feature
  cache has `unit="us"` and `asi8` returns microseconds; the matching
  `load` paths decoded with `dtype="datetime64[ns]"` unconditionally,
  silently shifting every timestamp by a factor of 1 000 and landing
  the loaded indices in 1970.  This was a Stage 12 D10 envelope-of-
  primitives migration regression: pre-skops the joblib pickle path
  carried the `DatetimeIndex` object whole and round-tripped its unit
  faithfully.  The Stage 11 ablation notebook surfaced the
  `NaiveModel` half (`ValueError: Seasonal-naive 'same_hour_same_weekday'
  found no training row matching weekday=...`); `NnTemporalModel`
  carries the same shape via its warmup-window prefix.  Regression
  guards: `tests/unit/models/test_naive.py::test_naive_save_load_round_trips_microsecond_unit_datetimeindex`
  and `tests/unit/models/test_nn_temporal_save_load.py::test_nn_temporal_save_load_round_trips_microsecond_unit_warmup_index`.

### Changed

- `tests/unit/models/test_nn_mlp_save_load.py::test_nn_mlp_save_writes_single_joblib_file_at_given_path`
  renamed to `..._single_skops_file_at_given_path`, matching the
  Stage 12 D10 canonical artefact filename and the sibling
  `NnTemporalModel` test (which was renamed in the Stage 12 PR).
  Stage 12 documentation drift in `src/bristol_ml/models/nn/mlp.py`
  module docstring, `src/bristol_ml/models/nn/temporal.py:1047`
  cross-reference, and `src/bristol_ml/models/nn/CLAUDE.md` (six
  references to "single-joblib envelope" / `model.joblib`) swept to
  use the post-Stage-12 skops vocabulary; historical context preserved
  inline with explicit "Stage 12 D10 — Ctrl+G reversal" attribution.

### Added

- Stage 16: `bristol_ml.features.remit` — new pure-derivation module producing the
  three REMIT-derived hourly columns for the `with_remit` feature set. Public
  surface: `REMIT_VARIABLE_COLUMNS` (typed column constant) and
  `derive_remit_features(remit_df, hourly_index, *, forward_lookahead_hours=24) ->
  pd.DataFrame`. Bi-temporal correctness is structurally enforced: each revision
  contributes only over the half-interval in which it is both the latest visible
  revision of its `mrid` **and** active per its event window; bypassing the
  function exposes the caller to leakage (NFR-1 / AC-1). Columns:
  `remit_unavail_mw_total` (float32 — MW sum of active events under the as-of
  rule), `remit_active_unplanned_count` (int32 — count of active unplanned events),
  `remit_unavail_mw_next_24h` (float32 — MW sum of events starting within the next
  24 hours, i.e. the forward-looking "known future input" signal per plan A2). Zero-
  event hours produce zeros; no NaN anywhere (AC-7). Algorithm uses delta-event
  aggregation + `merge_asof` for O((n_revisions + n_hours) log) vectorised
  evaluation instead of per-hour `as_of()` calls (plan D9). Standalone CLI
  `python -m bristol_ml.features.remit`. Stage 16 plan — `docs/plans/active/16-model-with-remit.md`.
- Stage 16: `bristol_ml.llm.persistence` — new module that adds the missing on-disk
  persistence step for Stage 14's extractor output (codebase hazard H1 — the
  `Extractor` Protocol returns `list[ExtractionResult]` in memory with no prior
  parquet). Public surface: `EXTRACTED_OUTPUT_SCHEMA` (11-column `pa.Schema` keyed
  on `(mrid, revision_number)`), `extract_and_persist(extractor, remit_df, *,
  output_path) -> Path` (atomic write via `ingestion._common._atomic_write`),
  `load_extracted(path) -> pd.DataFrame` (schema-exact read), and
  `DEFAULT_OUTPUT_PATH` (`data/processed/remit_extracted.parquet`). Standalone CLI
  `python -m bristol_ml.llm.persistence --cache {auto,refresh,offline} [--limit N]
  [--output PATH] [overrides...]` with `+llm=extractor` composed automatically;
  stub-default; live OpenAI path activated via `llm.type=openai` and
  `BRISTOL_ML_LLM_API_KEY`. Schema columns: `mrid`, `revision_number`, `event_type`,
  `fuel_type`, `affected_capacity_mw`, `effective_from`, `effective_to`,
  `confidence`, `prompt_hash`, `model_id`, `extracted_at_utc`.
- Stage 16: `with_remit` feature set — `WITH_REMIT_OUTPUT_SCHEMA` (59-column
  `pa.Schema`; 55-column `CALENDAR_OUTPUT_SCHEMA` prefix + 3 REMIT columns + 1
  `remit_retrieved_at_utc` provenance scalar), `assemble_with_remit(cfg, *, cache)
  -> Path`, and `load_with_remit(path) -> pd.DataFrame` added to
  `bristol_ml.features.assembler`. All three exported in `assembler.__all__`.
  `assemble_with_remit` composes the Stage 5 weather+calendar pipeline with the
  Stage 16 REMIT derivation; if the extracted-features parquet is absent it logs a
  WARNING and runs `extract_and_persist` inline under stub mode (CI affordance —
  see `features/CLAUDE.md` §"Stage 16 notes" for the explicit pre-population CLI).
  New `WithRemitFeatureConfig` Pydantic schema in `conf/_schemas.py` (fields:
  `forward_lookahead_hours: int = 24`, `include_forward_lookahead: bool = True`,
  `extracted_parquet_filename: str | None = None`) and
  `FeaturesGroup.with_remit: WithRemitFeatureConfig | None = None` slot. New
  `conf/features/with_remit.yaml` Hydra group file; select with `features=with_remit`
  at the CLI. New `with_remit` arm in `train._resolve_feature_set` (mutual-
  exclusivity invariant extended to the third feature set). The
  `include_forward_lookahead` flag determines which `feature_columns` the model
  reads; the on-disk schema always carries `remit_unavail_mw_next_24h` for
  contract stability (plan A2 / A4).
- Stage 16: `notebooks/04_remit_ablation.ipynb` — five-cell ablation notebook (1
  title-markdown + 3 executable code cells + 1 commentary markdown). Generated
  from `scripts/_build_notebook_16.py`. Ships with a runbook banner cell that
  detects absent `with_remit` registry runs (T6 deferred to the human's CUDA host)
  and prints the host-side commands. When T6 runs are present, renders a four-row
  metric table: best model without REMIT / best model with REMIT excluding
  `next_24h` / best model with REMIT including `next_24h` / NESO benchmark —
  rows 2-vs-1 isolate the marginal value of current-state REMIT signal; row 3-vs-2
  isolates the forward-looking column (plan A2). Smoke test at
  `tests/integration/test_notebook_04.py`.
- Stage 16: retrospective at [`docs/lld/stages/16-model-with-remit.md`](docs/lld/stages/16-model-with-remit.md),
  covering the Withdrawn-truncates-prior bug found during T2 stub-mode smoke,
  the dtype-precision mismatch on `merge_asof`, the auto-run extractor choice in
  `assemble_with_remit`, the T6 deferral, and the T6 host runbook.

- Stage 15: `bristol_ml.embeddings` — first **content-addressed cache** behind a typed, swappable boundary, and the project's first semantic-search surface. Public boundary (`bristol_ml/embeddings/__init__.py`): `Embedder` `runtime_checkable` `Protocol` with two methods + two properties (`embed(text) -> np.ndarray`, `embed_batch(texts) -> np.ndarray`, `dim`, `model_id`); `VectorIndex` `runtime_checkable` `Protocol` with four methods + one property (`add(ids, vectors)`, `query(vec, k) -> list[NearestNeighbour]`, `save(path)`, `load(path)` classmethod, `dim`); `NearestNeighbour` `NamedTuple(id: str, score: float)` for tight-loop query returns; `EmbeddingCache` + `EmbeddingCacheMetadata` frozen dataclasses; `synthesise_embeddable_text` for NULL-`message_description` fallback; `STUB_ENV_VAR` (`"BRISTOL_ML_EMBEDDING_STUB"`) and `MODEL_PATH_ENV_VAR` (`"BRISTOL_ML_EMBEDDING_MODEL_PATH"`) constants. ADR-0008 records the Protocol-over-ABC choice; ADR-0003 set the precedent for `Model`, Stage 14 reused it for `Extractor`, Stage 15 establishes it as the project's house style for swappable interfaces. Two implementations of each Protocol (`bristol_ml/embeddings/_embedder.py` and `_index.py`): `StubEmbedder` (offline-by-default; deterministic 8-dim SHA-256-derived L2-normalised float32 vectors; mirrors the `"query: "` prefix asymmetry — query/document path divergence — so a regression where a downstream caller routes queries through `embed_batch` surfaces in unit tests rather than as silent retrieval-quality degradation; AC-4 / NFR-1) and `SentenceTransformerEmbedder` (live path; loads `Alibaba-NLP/gte-modernbert-base` 149 M params 768-dim with `model_kwargs={"torch_dtype": torch.float16}` halving the ~298 MB safetensors fp32 footprint to ~149 MB; deferred `from sentence_transformers import SentenceTransformer` so the boundary import stays light — Stage 16 callers holding the Protocol type pay no `sentence-transformers` import cost — pinned by `tests/unit/embeddings/test_protocol.py::test_boundary_import_does_not_pull_sentence_transformers`); `StubIndex` (in-memory list-of-pairs minimal conformance for unit tests) and `NumpyIndex` (production binding; pre-normalised float32 corpus matrix; cosine query is a single `corpus @ query.T` matmul; `np.savez_compressed` persistence with the `.tmp` sibling + `os.replace` atomic-write idiom borrowed from `bristol_ml.ingestion._common._atomic_write`).
- Stage 15: `bristol_ml.embeddings._cache.EmbeddingCache` — content-addressed cache at `data/embeddings/<model_id_sanitised>.parquet` (gitignored). `load_or_build(path, ids, texts, embedder, force_rebuild=False)` is the cache's single public entry; tries to read the parquet, recomputes `corpus_sha256` from the supplied texts (full 64-char hex over the deterministic `"\n"`-joined serialisation), compares against the stored hash *and* the supplied `embedder.model_id`, rebuilds on either mismatch with a `loguru` WARNING naming the offending field. Three provenance fields stamped into Parquet `custom_metadata`: `embedded_at_utc` (UTC ISO-8601 string), `corpus_sha256` (stored full-length, exposed as a 12-char prefix on `EmbeddingCacheMetadata.corpus_sha256_prefix` for log-line ergonomics — collisions over the project lifetime are vanishing at 12 hex chars / 48 bits / 1 in 281 trillion), `model_id`. `git_sha` was the Scope-Diff cut (no AC; duplicates registry-layer convention for a cache file). The lessons generalise beyond embeddings: any cached derivation downstream of an ingested corpus copies the same shape — content-hash + backend-id as the invalidation key, Parquet `custom_metadata` as the provenance carrier, deterministic recompute on mismatch (NFR-2 / NFR-5 / AC-3 / AC-8). Cache vectors stay float32 even when inference is fp16 — fp16 inference can introduce minor numeric jitter that would surface as non-deterministic top-k ordering when ties are close (R-3).
- Stage 15: `bristol_ml.embeddings._factory` — `build_embedder(config) -> Embedder`, `build_index(config, *, dim) -> VectorIndex`, and the high-level glue `embed_corpus(*, config, corpus, id_columns=("mrid", "revision_number")) -> tuple[VectorIndex, EmbeddingCache]`. **Triple-gated dispatch** (plan §1 D8) for the embedder factory: `BRISTOL_ML_EMBEDDING_STUB=1` env-var beats YAML discriminator (`type: stub` default in `conf/embedding/default.yaml`) beats explicit live config — load-bearing for CI safety, mirroring Stage 14's `BRISTOL_ML_LLM_STUB` discipline. The `BRISTOL_ML_EMBEDDING_MODEL_PATH` env-var optionally overrides `config.model_id` for the live path. `embed_corpus` ties text-synthesis (`synthesise_embeddable_text` over each row) + cache resolution (default path derived from `model_id` at call time via filesystem-safe sanitiser; `data/embeddings/<sanitised>.parquet`) + factory + `index.add(cache.ids, cache.vectors)` into one call site that the notebook and the standalone CLI both use. Index id grain is `"<mrid>::<revision_number>"` — Stage 13's `OUTPUT_SCHEMA` primary-key pair uniqueness guarantees no de-duplication is needed.
- Stage 15: `bristol_ml.embeddings._text.synthesise_embeddable_text(row)` — NULL-aware text-coercion pass over Stage 13 REMIT rows (mechanical reuse of Stage 14's OQ-B resolution). Returns `row.message_description` when non-NULL and non-empty after strip; falls back to a single-space join of the non-NULL subset of `event_type`, `cause`, `fuel_type`, and `affected_unit` when `message_description` is NULL but structural fields are present; returns the empty string `""` when every structural field is also NULL (pinned by `test_fully_null_row_returns_empty_string` — callers wanting to drop empty rows pre-embed can filter on the empty return). Handles every pandas NULL flavour (`pd.NA`, `pd.NaT`, `np.nan`, `None`) via `pd.isna`-with-scalar-guard.
- Stage 15: `bristol_ml.embeddings._projection.project_to_2d(vectors, *, random_state=42, n_neighbors=15, min_dist=0.1)` — UMAP wrapper for the notebook's 2D-projection cell. Pins `n_jobs=1` because UMAP's `random_state` is coordinate-exact-reproducible only when single-threaded (per umap-learn docs). `metric="cosine"`. Lazy `from umap import UMAP` import so a stub-only or factory-only caller never pays the ~200 MB compiled `numba` + `llvmlite` import cost. Plan A3 (Ctrl+G flip from PCA): the user's argument was that PCA's two principal components do not preserve neighbourhood structure in 768-dim embedding space, which would undermine the demo moment ("the 2D projection shows the corpus falling into visible clusters by event type and fuel"). Dependency cost accepted.
- Stage 15: `python -m bristol_ml.embeddings` standalone CLI — composes `+embedding=default` automatically (the `embedding` group is *not* in `conf/config.yaml`'s defaults — parallel to `llm` and `serving`); prints the resolved config (model_id, dim, vector_backend, projection_type, env-var state); builds a synthetic 5-row in-memory index from hand-picked REMIT-flavoured texts; runs a one-shot top-3 query against `"planned nuclear outage"`; exits 0. Output is byte-deterministic in stub mode so the smoke test (`tests/unit/embeddings/test_module_runs_standalone.py::test_module_runs_under_stub`) can pin expected lines. A4 narrowed the CLI to the standalone-module entry only — no `build` / `query` subcommands. Plan §1 A4: intent line 45 phrases the full CLI as "useful, not mandatory"; the notebook plus the standalone entry covers both demo paths.
- Stage 15: new `EmbeddingConfig` Pydantic schema in `conf/_schemas.py` (`type: Literal["stub", "sentence_transformers"]` discriminator, `model_id: str | None`, `cache_path: Path | None`, `vector_backend: Literal["numpy", "stub"]`, `default_top_k: int ∈ [1, 1000]`, `projection_type: Literal["umap"]`, `force_rebuild: bool`, `fp16: bool`) and `AppConfig.embedding: EmbeddingConfig | None = None` slot — the `None` default keeps every prior stage's CLI / config-smoke test unaffected (the train pipeline never resolves `cfg.embedding`). `conf/embedding/default.yaml` Hydra group file with `type: stub` so the offline path is the runtime default; group is *not* in `conf/config.yaml`'s defaults (composed via `+embedding=default`). `tests/unit/test_config.py::test_embedding_config_round_trips_through_hydra` pins the schema round-trip (T7).
- Stage 15: `tests/fixtures/embedding/tiny_corpus.parquet` — 8-row REMIT-shaped fixture spanning four fuel types (Nuclear ×2, Gas ×2, Coal ×2, Wind ×2) with three NULL `message_description` rows (M-AA, M-C, M-D) exercising the structural-fallback path on the fixture itself. Built by `scripts/_build_embedding_fixture.py` against the Stage 13 `OUTPUT_SCHEMA` so a drift between this fixture and the upstream schema is caught at the next plan-edit conversation.
- Stage 15: `notebooks/15_embedding_index.ipynb` — eight-cell demo notebook (1 title-markdown + 6 plan-§5 code cells + 1 closing-discussion markdown). Generated programmatically from `scripts/_build_notebook_15.py`. Cell 1 (`T5 Cell 1`) sets `os.environ["BRISTOL_ML_EMBEDDING_STUB"]="1"` *before* importing any embeddings code and walks up to find `pyproject.toml` for repo-root anchoring; Cell 2 (`T5 Cell 2`) loads the tiny REMIT corpus fixture and prints NULL-`message_description` ratio; Cell 3 (`T5 Cell 3`) calls `embed_corpus(...)` into a per-run `tempfile.mkdtemp(prefix="stage15_embedding_cache_")` cache (so notebook execution does not pollute the human's main `data/embeddings/` directory) and prints the provenance fields (corpus_sha256_prefix, model_id, embedded_at_utc); Cell 4 (`T5 Cell 4`) picks a fixed gold event id and prints ten nearest neighbours with cosine scores as a pandas frame; Cell 5 (`T5 Cell 5`) UMAP scatter coloured by `fuel_type` with `n_neighbors_eff = max(2, min(15, n_samples - 1))` to handle the fixture's 8-row corpus without tripping UMAP's `n_neighbors >= n_samples` assertion; Cell 6 (`T5 Cell 6`) is the **optional cross-stage join** (plan A5) — when `data/features/remit_extracted.parquet` (Stage 14 output) is present, joins extracted `event_type` / `fuel_type` columns onto the top-k; when absent (the default in stub-mode CI), prints a single-line skip banner. Smoke test at `tests/integration/test_notebook_15.py` runs `nbconvert --execute` under `BRISTOL_ML_EMBEDDING_STUB=1` + `HF_HUB_OFFLINE=1` (180 s timeout); asserts T5 Cell 1 / 3 / 4 / 5 each produce non-empty output, plus a `test_notebook_15_has_expected_cell_count[8]` cell-shape sanity check.
- Stage 15: 13-test embeddings unit suite at `tests/unit/embeddings/`. `test_protocol.py` covers Protocol shape (2 + 4 method counts pinned), `isinstance(_, Embedder)` / `isinstance(_, VectorIndex)` for both stubs, the boundary-import assertion that `sentence_transformers` is *not* in the import graph after `import bristol_ml.embeddings`, and `NearestNeighbour` namedtuple shape; carries a 12-line implementation note documenting the cross-suite torch-hygiene constraint (do NOT evict `torch` from `sys.modules` even temporarily — torch's `__init__.py` registers `torch.library.Library('triton', 'DEF')` at module level, and a re-import after eviction triggers a global-state `RuntimeError: Only a single TORCH_LIBRARY can be used to register the namespace triton` that cascades into every torch-using test in the suite). `test_stub_embedder.py` covers env-var triple-gate dispatch, determinism (same text → bit-identical vector), and prefix asymmetry between `embed` (query path with `"query: "` prepend) and `embed_batch` (document path, no prefix). `test_st_embedder.py` covers live-embedder conformance against pre-warmed local cache; xfail / skip when cache absent (CI default — the test does not download). `test_index_query.py` covers top-k sorted descending, cosine scores in `[-1, 1]`, save/load round-trip, empty-index returns `[]`, k > index-size silently clipped. `test_cache_invalidation.py` covers the four cache pathways (fresh cache no-op; corpus-hash mismatch triggers rebuild + WARNING; model-id mismatch triggers rebuild + WARNING; `force_rebuild=True` overrides) plus the atomic-write contract (no `.tmp` artefacts on success). `test_text_synthesis.py` covers non-NULL pass-through, NULL fallback to structured fields, every-field-NULL sentinel, and pandas NULL-flavour coercion. `test_module_runs_standalone.py` covers `python -m bristol_ml.embeddings` exit-0 + expected line presence under stub mode.
- Stage 15: `docs/architecture/decisions/0008-embedding-index-protocol.md` — new ADR; `Embedder` + `VectorIndex` are `runtime_checkable` Protocols, not ABCs. Three swappable interfaces (`Model`, `Extractor`, `Embedder` + `VectorIndex`) now use the same pattern; ADR-0008 establishes it as the project's house style for swappable interfaces whose conformance is a method-shape question. Supersession criteria recorded: a future `embed_async` method or a behavioural-conformance interface would warrant a new ADR.
- Stage 15: `docs/architecture/layers/embeddings.md` — new layer doc covering the Protocol contract, the env-var triple-gate, the content-hash cache discipline (corpus_sha256 + model_id as the invalidation key, Parquet `custom_metadata` as the provenance carrier), the `"query: "` prefix asymmetry, the `Alibaba-NLP/gte-modernbert-base` live binding (149 M params, 768-dim, MTEB-56 64.38, fp16 inference), the UMAP projection rationale (`n_jobs=1` for coordinate-exact reproducibility), the optional cross-stage cell, and the standalone CLI shape. Status: Provisional (first realised by Stage 15). `docs/architecture/README.md` ADR/layer index gained the Embeddings row and the ADR-0008 entry. `src/bristol_ml/embeddings/CLAUDE.md` — module guide: schema reference, cache file layout on disk, model pre-warm ritual (one-line `SentenceTransformer('Alibaba-NLP/gte-modernbert-base')` against a network-online host before the meetup), how-to-add-a-third-backend recipe (one literal extension + one dispatch branch + one Protocol-conformance test).
- Stage 15: retrospective at [`docs/lld/stages/15-embedding-index.md`](docs/lld/stages/15-embedding-index.md) including the observed fixture composition (8 rows, 4 fuel types, 3 NULL `message_description`), the pre-emptive deviation on the boundary-import test (the torch-hygiene constraint that emerged during a 49-test cascade-failure debugging session), the `embed_corpus` 2-tuple return-shape clarification, and the live-model not-pre-warmed build-up phase decision (mirrors Stage 14's cassette-not-recorded pattern).
- Stage 15: `pyproject.toml` runtime deps gained `sentence-transformers>=3.3,<5` (brings `transformers >= 4.48.0` required for ModernBERT support; `tokenizers`, `huggingface_hub` transitive) and `umap-learn` (notebook 2D projection per A3; brings `numba` + `llvmlite` + `pynndescent`). No FAISS, no LanceDB, no chromadb. `tests/conftest.py` sets `HF_HUB_OFFLINE=1` at module-import time so any sentence-transformers / huggingface_hub import in the test process refuses to fetch over the network — belt-and-braces with the project's stub-first triple gate (a regression that re-introduces a network call surfaces here as `OSError` rather than a silent download).

- Stage 14 (Phase 3 review fixes):
  - **Blocking — API-key partial leak in `LlmExtractor` failure logs.** `OpenAI`'s `AuthenticationError` echoes a partial key (e.g. `sk-real*****CDEF`) in its server-supplied message; the previous WARNING + DEBUG lines logged `exc` and `{!r}`, landing that fragment in operator logs. `src/bristol_ml/llm/extractor.py` now logs only `type(exc).__name__` and the event mrid / revision_number — preserving operability while honouring DESIGN §7's "secrets must not appear in code, config, or logs" discipline. Regression guard: `tests/unit/llm/test_llm_extractor.py::test_failure_log_does_not_leak_exception_message_or_repr` injects a `RuntimeError("Incorrect API key provided: sk-realk1234EXAMPLECDEF.")` and asserts the leaky fragment is absent from every captured log record.
  - **Major — Double extraction of the first gold-set record in `evaluate()`.** The harness's main loop extracted every record once; a second `extractor.extract(gold_set[keys[0]].event)` call after the loop read `prompt_hash` / `model_id` for the report header, costing one redundant OpenAI request per harness run. `src/bristol_ml/llm/evaluate.py` now captures the first iteration's result inline and reuses it for the header. Regression guard: `test_evaluate_calls_extract_exactly_once_per_record` counts `extract` invocations against an N-record fixture and asserts exactly N calls.
  - **Major — Stale Anthropic references in `docs/plans/completed/14-llm-extractor.md`.** OQ-A bound the live provider to OpenAI at Ctrl+G review, but five lines in the plan still read as if Anthropic shipped (D18 cassette endpoint, D78 "ships only stub and anthropic", line 109 "calling Anthropic Claude Haiku 4.5", line 350 `llm.type=anthropic` example, OQ-5 reading-order note, D14 schema sketch `claude-haiku-4-5`). All five updated to reflect what shipped (OpenAI Chat Completions strict mode against `api.openai.com/v1/chat/completions`; `gpt-4o-mini`; `llm.type=openai`). Historical references (D3 future-slot example, OQ-A drafted-default note, R1.1 researcher-doc reference) preserved as accurate context.
  - **Minor — `"...and N more"` disagreement count understated when `_accumulate_per_field` overflows the cap.** Each record can append up to five disagreement items; the gate `len(disagreements) <= max_disagreements` permitted overshoot but `format_report` reported `remaining = len(disagreements) - max_disagreements` — counting only captured items, not total observed. With 10 disagreements observed and `max_disagreements=1`, output read `... and 4 more` instead of the correct `... and 9 more`. `_accumulate_per_field` now returns `observed: int` (0-5) regardless of capture; `evaluate()` sums into a new `EvaluationReport.total_disagreements` field; `_format_disagreements` uses `report.total_disagreements - len(shown)`. Regression guard: `test_total_disagreements_count_is_accurate_when_capture_overflows` (2 records × 5 fields all wrong, `max_disagreements=1` ⇒ asserts `total_disagreements == 10` and output contains `... and 9 more`).
  - **Minor — `DEFAULT_GOLD_SET_PATH` and `prompt_file` were cwd-relative.** Both paths previously resolved against the process working directory, breaking notebook callers (cwd = `notebooks/`) and Stage 15/16 importers from elsewhere on the filesystem. `DEFAULT_GOLD_SET_PATH` now anchors on `Path(__file__).resolve().parents[3]` (repo root); `LlmExtractor.__init__` now anchors a relative `config.prompt_file` against the same root before passing to `load_prompt`. Regression guard: `test_stub_loads_canonical_gold_set_from_alternate_cwd` `chdir`s to a temp directory and constructs `StubExtractor()` without a path override.
  - **Minor — `prompt_sha256_full` exported but unused.** Plan-polish surface: the function had no consumer in this stage's diff, justified speculatively as "if a future stage needs to join onto an external prompt registry". Cut from `_prompts.__all__` and from the public surface; the layer doc and module guide note that the full digest is reconstructible from input bytes via `hashlib.sha256(prompt_bytes).hexdigest()` if a future stage actually needs it.
  - **Housekeeping — stale `docs/plans/active/14-llm-extractor.md` link in source-file docstrings.** The plan moved to `completed/` at T8; six docstrings in `src/bristol_ml/llm/__init__.py`, `src/bristol_ml/llm/extractor.py`, and the four test files still pointed at `active/`. All six updated to `completed/`.
- Stage 14: `bristol_ml.llm` — first **authenticated outbound dependency** behind a typed, swappable boundary. Public surface (`bristol_ml/llm/__init__.py`): `Extractor` `runtime_checkable` `Protocol` with exactly two methods (`extract` / `extract_batch`); `RemitEvent` Pydantic model (typed mirror of Stage 13's `OUTPUT_SCHEMA` row, UTC-aware datetime validators, `extra="forbid"`, `frozen=True`); `ExtractionResult` Pydantic model (`event_type`, `fuel_type`, `affected_capacity_mw`, `effective_from`, `effective_to`, `confidence ∈ [0, 1]`, plus provenance pair `prompt_hash` + `model_id`). Two implementations (`bristol_ml/llm/extractor.py`): `StubExtractor` (offline-by-default; loads `tests/fixtures/llm/hand_labelled.json` at init; indexes by `(mrid, revision_number)`; returns labelled extraction on hit, documented default with `confidence=0.0` on miss; AC-2 / NFR-1) and `LlmExtractor` (live OpenAI Chat Completions strict mode via `response_format={"type": "json_schema", "strict": true, "schema": ...}` — CFG token masking, GA since August 2024; hand-authored `_OPENAI_RESPONSE_SCHEMA` because `Pydantic.model_json_schema()` emits `default: null` on `Optional[...]` fields which strict mode rejects; on any network/parse/validation failure logs WARNING + returns documented default with provenance stamped — `extract` never raises (NFR-6)). `build_extractor(config)` factory dispatches on `config.type` *and* the `BRISTOL_ML_LLM_STUB` env var — **triple-gated** so no path can fire a live OpenAI call by accident: YAML `type: stub` default + env-var override `BRISTOL_ML_LLM_STUB=1` + API-key gate at `LlmExtractor.__init__` (raises `RuntimeError` naming both env-vars when key missing). `bristol_ml/llm/_prompts.py` provides SHA-256 prompt-bytes hashing with 12-char hex prefix as the minimum-viable prompt-versioning identity (NFR-5; collision odds 1 in 281 trillion). New `LlmExtractorConfig` Pydantic schema (`type: Literal["stub", "openai"]` discriminator, `model_name`, `api_key_env_var`, `prompt_file`, `request_timeout_seconds`) and `AppConfig.llm: LlmExtractorConfig | None = None` slot in `conf/_schemas.py`; `conf/llm/extractor.yaml` Hydra group file with `type: stub` default; the group is *not* in `conf/config.yaml`'s defaults (composed via `+llm=extractor` parallel to `serving`). Active prompt at `conf/llm/prompts/extract_v1.txt`. Standalone CLI `python -m bristol_ml.llm.extractor [overrides...]` exits 0 with a deterministic one-extraction summary (NFR-8 / DESIGN §2.1.1).
- Stage 14: `bristol_ml.llm.evaluate` — evaluation harness that runs the active extractor over the gold set and prints a deterministic side-by-side accuracy report (the demo moment, intent §"Demo moment", line 28). Output: header (implementation name, prompt hash, model id, gold-set sha256, gold-set size, generated_at) → per-field summary (exact-match + tolerance-match counts side-by-side; ±5 MW capacity / ±1 h timestamps per plan §1 D12; categorical fields have exact == tolerance) → first-N disagreements (default 10; `--max-disagreements N` to adjust). Two-metric design (plan §1 D11) so the same data produces different numbers under different metric choices — the metric-choice lesson (intent line 41). Reproducibility (NFR-3): `--redact-timestamps` flag replaces the single wall-clock header line with a fixed sentinel; `tests/unit/llm/test_evaluate.py::test_evaluate_harness_output_is_deterministic_modulo_timestamp` asserts byte-equality across two runs with different `now=` arguments. Captured trace (Ctrl+G OQ-D / D15): the same content is emitted as a single structured `loguru` INFO record via `logger.bind(summary={...}).info("Stage 14 evaluation: implementation=... gold_set_size=... disagreements=...")` so a non-notebook CLI run leaves a captured trace. Standalone CLI `python -m bristol_ml.llm.evaluate [overrides...] [--redact-timestamps] [--max-disagreements N]` (NFR-8). 10 unit tests at `tests/unit/llm/test_evaluate.py` covering AC-4 round-trip, byte-determinism, provenance header, capacity tolerance (`_DriftExtractor` injection via monkey-patching `eval_mod.build_extractor`), datetime tolerance (`_TimeDriftExtractor`), disagreement listing shape, `--max-disagreements` truncation with `... and N more` suffix, standalone CLI exit 0, CLI runs against the stub via `argv` with stdout capture, and `loguru` summary record emission via the `loguru_caplog` bridge fixture.
- Stage 14: `tests/fixtures/llm/hand_labelled.json` — 76-record hand-labelled gold set (schema_version 1) stratified across event type (Outage 51, Restriction 25) and 11 fuel types (Gas 18, Nuclear 13, Wind 13, Solar 8, Coal 6, Hydro 5, Pumped Storage 4, Biomass 3, Oil 2, Interconnector 2, Battery 2). Indexed by `(mrid, revision_number)` — the same primary key Stage 13's REMIT parquet uses. Synthesise-on-NULL probes M-NA..M-ND (`message_description=None`) and missing-capacity probe M-NM (`affected_mw=None`) exercise the live extractor's NULL-tolerance path (plan §1 D6 / Ctrl+G OQ-B). Just below the researcher-R3 reference of 100 records (±10 % margin at 95 % CI / 80 % accuracy); inside intent line 40's "dozens to low hundreds" range; landed at 76 vs OQ-C's target 80 to keep curation effort proportional.
- Stage 14: `tests/integration/llm/test_llm_extractor_cassette.py` — VCR cassette test against `api.openai.com/v1/chat/completions` covering 5 strata-spanning gold-set events (M-A Nuclear, M-B Gas, M-C Coal, M-H synthesise-on-NULL probe, M-I Solar). `vcr_config` filters `authorization`, `cookie`, `set-cookie`, `x-api-key` headers (mirrors the project's existing fixture from `tests/integration/ingestion/test_remit_cassettes.py`). Replay-only under the project default `--record-mode=none`; **skips when the cassette is absent** (build-up phase decision: no API key in the dev environment at Stage 14 ship; cassette refresh ritual documented in `src/bristol_ml/llm/CLAUDE.md`). Asserts cardinality preservation, every result is an `ExtractionResult`, `prompt_hash` is the 12-char SHA-256 prefix, `model_id == config.model_name`, `confidence ∈ [0, 1]`. The harness (T5) is the place for accuracy assertions; the cassette test pins structural correctness.
- Stage 14: `notebooks/14_llm_extractor.ipynb` — seven-cell demo notebook (1 title-markdown + 6 plan-§5 cells: bootstrap + load config; load gold set + per-fuel breakdown; run stub on 3 sample events; live-path readiness banner; harness side-by-side with pandas-frame disagreement view; markdown discussion of metric choice). Generated programmatically from `scripts/_build_notebook_14.py`. Cell 3 explicitly sets `os.environ["BRISTOL_ML_LLM_STUB"]="1"` so the notebook is deterministic regardless of YAML defaults. Cell 4 prints one of three banners explaining whether a live run is ready (cassette present / API key set / stub override active) — the banner-not-error pattern lets the notebook execute top-to-bottom with or without an API key. End-to-end execution under stub mode: ~10 seconds. Smoke test at `tests/integration/test_notebook_14.py` runs `nbconvert --execute` under `BRISTOL_ML_LLM_STUB=1`, asserts T5 Cell 1 / 3 / 5 produce non-empty output, plus a parametrised `test_notebook_14_has_expected_cell_count[7]` cell-shape sanity check.
- Stage 14: `docs/architecture/layers/llm.md` — new layer doc covering the `Extractor` Protocol contract, the env-var triple-gate (config discriminator + `BRISTOL_ML_LLM_STUB` + API-key presence), the prompt-hash provenance convention, the strict-mode JSON-schema gotcha (Pydantic emits `default: null` on `Optional` fields; OpenAI strict mode rejects), the `message_description` NULL strategy (plan D6 / OQ-B), the gold-set fixture format, the cassette-refresh ritual, and the side-by-side harness output. `docs/architecture/README.md` ADR/layer index gained the LLM row. `src/bristol_ml/llm/CLAUDE.md` — module guide: schema reference for `RemitEvent` and `ExtractionResult` (with the downstream-consumer warning that `confidence` is a documented sentinel — 1.0 stub-hit / 0.0 fallback / LLM-emitted otherwise — *not* a calibrated probability), cassette-refresh ritual + leak-check command, gold-set curation guidance (mRID prefix conventions, when to bump the size). `README.md` gained a new "Worked example: LLM feature extractor (Stage 14)" section linking the layer doc, the module guide, and the retro; the existing "API keys and offline-by-default" section already documents the `BRISTOL_ML_LLM_API_KEY` setup process (added at Phase 1 follow-up per Ctrl+G OQ-D).
- Stage 14: retrospective at [`docs/lld/stages/14-llm-extractor.md`](docs/lld/stages/14-llm-extractor.md), including the observed gold-set size (76), the deliberate `--max-disagreements N` deviation from plan §6 T5 (added for live-path legibility), the cassette-not-recorded build-up decision, and the OpenAI SDK lazy-import discipline (deferred `from openai import OpenAI` so a stub-only test environment imports the module without error).
- Stage 14 (Phase 1 follow-up): `README.md` — new "API keys and offline-by-default" section documenting the `BRISTOL_ML_LLM_API_KEY` setup process, the `BRISTOL_ML_LLM_STUB=1` force-stub override, and the standard env-var table extended with the three project-scoped variables (`BRISTOL_ML_LLM_API_KEY`, `BRISTOL_ML_LLM_STUB`, `BRISTOL_ML_REMIT_STUB`, `BRISTOL_ML_CACHE_DIR`). Added per Ctrl+G 2026-04-27 follow-up to the Stage 14 plan: the human asked for the API-key setup to be in the user-facing README, not buried in the layer doc.

- Stage 13: `bristol_ml.ingestion.remit` — first **bi-temporal** ingester. Public surface: `OUTPUT_SCHEMA` (16-column pyarrow schema), `MESSAGE_STATUSES` / `FUEL_TYPES` constants, `fetch(config, *, cache=CachePolicy.AUTO) -> Path`, `load(path) -> pd.DataFrame`, and the new primitive `as_of(df, t) -> pd.DataFrame`. Storage shape: append-only at `(mrid, revision_number)` grain with three publish-axis times (`published_at` for transaction-time, `effective_from` / `effective_to` for valid-time; `effective_to` nullable for open-ended events) plus the standard `retrieved_at_utc` per-fetch provenance scalar — all four stored as `timestamp[us, tz=UTC]`. The `as_of(df, t)` algorithm is three lines of pandas: filter `published_at <= t` (transaction-time), `groupby(mrid).idxmax(revision_number)`, drop rows whose `message_status == "Withdrawn"`. Strictly transaction-time — callers wanting "events active at `t`" (valid-time) chain a second filter on `effective_from` / `effective_to`. Naive timestamps raise `ValueError`. Live fetch path issues a single GET against `/datasets/REMIT/stream` (no paging — the streaming endpoint serves the whole window in one response, domain research §R3); 15-field camelCase → snake_case parser handles the actual live-API `eventStatus → message_status` field naming (research draft §R2 had transcribed it as `messageStatus`). loguru INFO record per fetch carries the record count + window slice (NFR-10); WARNING on unknown `message_status` values. `_common.py` helpers reused verbatim via the structural-Protocol contract on `RemitIngestionConfig`. Stub mode (`BRISTOL_ML_REMIT_STUB=1`) routes fetch through 10 hand-crafted records covering all four AC-1 cases (fresh / revised / withdrawn / open-ended) for offline CI runs. New `RemitIngestionConfig` Pydantic schema and `IngestionGroup.remit: RemitIngestionConfig | None = None` slot in `conf/_schemas.py` (the `None` default keeps every prior ingester unchanged); `conf/ingestion/remit.yaml` Hydra group file; `conf/config.yaml` `defaults:` extended with `- ingestion/remit@ingestion.remit`. Module CLI `python -m bristol_ml.ingestion.remit --help` prints the resolved schema. 24 tests (18 unit at `tests/unit/ingestion/test_remit.py` covering AC-1 four cases + naive-timestamp guard + `OUTPUT_SCHEMA` + stub-fetch round-trip + four-timestamp `tz=UTC` + `pd.NaT` open-ended + `CacheMissingError` + standalone CLI + Phase-3 review-driven structural guards on `_live_fetch` URL slash-handling, default `window_end` UTC source, `RemitParseError` typed payload-shape failures, missing-required-field parse error, and a warm-cache offline round-trip that monkeypatches `httpx.Client` to a no-network stub; 4 integration at `tests/integration/ingestion/test_remit_cassettes.py` covering AC-3 cassette populates cache, AC-4/NFR-1 idempotent re-fetch row-identical modulo `retrieved_at_utc`, AC-1 `as_of` invariants at three sample times with exact `(9, 64, 70)` mRID counts and a strict-subset chain across the three points, NFR-10 INFO log; 2 integration at `tests/integration/test_notebook_13.py` for the notebook smoke-test plus a cell-count sanity check).
- Stage 13: `notebooks/13_remit_ingestion.ipynb` — seven-cell demo (4 markdown + 3 code) generated from `scripts/_build_notebook_13.py`. The demo moment is Cell 5: stacked-area MW by fuel type, month-start as-of. Cell 1 fetches and loads (under `BRISTOL_ML_REMIT_STUB=1` for offline runs); Cell 3 iterates month-starts calling `as_of` then a valid-time predicate `(effective_from <= t) & (effective_to.isna() | (effective_to > t))`, groupby `fuel_type` sum `affected_mw` to build the long-form aggregate; Cell 5 pivots and renders the chart. End-to-end execution under stub mode: ~5 seconds. A facilitator can point at a spike on the chart and say "that was a nuclear unit going offline on this date", then drop into the frame in Cell 3 to confirm.
- Stage 13: `tests/fixtures/remit/cassettes/remit_2024_01_01.yaml` — single VCR cassette covering 2024-01-01 → 2024-01-02 of `/datasets/REMIT/stream` (~125 records / 70 mRIDs / **31 in-window revision chains** — exercises `as_of` against real revision behaviour, not synthetic). **Cassette size: 20 220 bytes (~20 kB)**, comfortably inside the plan's 100 kB budget. Window narrowed from the plan-drafted one-week to one-day because the live API's record density (~125 records/day) would have produced a ~1.8 MB raw payload across a week, exceeding the budget. The synthetic-withdrawal case stays at unit level via `test_as_of_withdrawn_message_excludes_row` (a `Withdrawn` row is rare in any naturally selected window and would balloon the cassette if forced).
- Stage 13: `docs/architecture/layers/ingestion.md` — flipped from "Provisional — revisit after Stage 13" to "Stable for two storage shapes" (single-timestamp level data; four-timestamp event data). New §"Bi-temporal storage shape" section documents the four-column model, the append-only revision log, the `as_of` algorithm, and a worked "published / revised / withdrawn — what does as-of(t) return?" example covering four sample times. Module inventory row for `remit.py` flipped from Planning to Shipped. The "bi-temporal storage (Stage 13)" open question marked resolved with a back-reference. `src/bristol_ml/ingestion/CLAUDE.md` extended with the REMIT row in the schema-table family + a new "as-of query" subsection + stub-mode and cassette-scope notes. README gained a new "Worked example: REMIT bi-temporal ingestion" paragraph.
- Stage 13: retrospective at [`docs/lld/stages/13-remit-ingestion.md`](docs/lld/stages/13-remit-ingestion.md), including the observed cassette size (20 kB), the 31 in-window revision chains, the three Scope Diff cuts (D15 BMU reference helper — *single highest-leverage cut*; D17 ADR deferred; D18e duplicate uniqueness test cut), and the one observed deviation from plan (cassette window narrowed from one-week to one-day for budget reasons).
- Stage 13 (Phase 3 review fix B-1): [`docs/architecture/decisions/0007-ingestion-public-contract-bifurcates-by-data-shape.md`](docs/architecture/decisions/0007-ingestion-public-contract-bifurcates-by-data-shape.md) — new ADR recording that the ingestion-layer public contract bifurcates by data shape: level-data ingesters expose `fetch` + `load`; event-log ingesters additionally expose at least one temporal-query primitive (canonical: `as_of(df, t)`). The trigger that makes a source "event-log" is multiple temporal axes per row (transaction-time + valid-time minimum). Resolves the inconsistency between the `arch-reviewer`'s B-1 finding ("REMIT ships three public callables; layer doc says exactly two") and the underlying structural reality (level vs event-log shapes differ, so a uniform contract would force four files of degenerate `as_of` boilerplate on level-data ingesters). [`docs/architecture/layers/ingestion.md`](docs/architecture/layers/ingestion.md) §2 "Public interface" replaces the "exactly two public callables" clause with a two-row data-shape table back-referencing the ADR; §"What lives here" intro and §"Upgrade seams" intro updated for parallel consistency. NESO / weather / holidays / neso_forecast unchanged. [`docs/architecture/README.md`](docs/architecture/README.md) ADR index gained the 0007 row.

- Stage 12: `bristol_ml.serving` — minimal FastAPI prediction endpoint loading from the Stage 9 registry. `build_app(registry_dir: Path) -> FastAPI` is the public factory. The lifespan resolves the lowest-MAE registered run across **all six** model families (naive, linear, sarimax, scipy_parametric, nn_mlp, nn_temporal — D9 Ctrl+G reversal: every family is eligible, including nn_temporal via Stage 11's warmup-envelope) and stashes it in `app.state.loaded`. `GET /` returns the default-model summary; `POST /predict` is features-in (D4), `AwareDatetime`-typed target_dt normalised to UTC (D8), default-or-supplied `run_id` (D5), lazy-loaded non-default runs cached in `app.state.loaded` (D7 single highest-leverage cut). Unknown `run_id` → HTTP 404 with detail naming the missing id and the registry directory. Empty registry → `RuntimeError` at lifespan startup with the registry path in the message (AC-1). Per-request seven-field structured log via `loguru.logger.bind(...).info("served prediction")` (D11 — `request_id`, `model_name`, `model_run_id`, `target_dt`, `prediction`, `latency_ms`, `feature_hash` — the load-bearing contract Stage 18 drift monitoring will consume). OpenAPI 3.0 document auto-emitted by FastAPI with `PredictRequest` and `PredictResponse` as named components (AC-4). Standalone CLI `python -m bristol_ml.serving` with `--registry-dir`, `--host`, `--port`, and Hydra `overrides` positional (DESIGN §2.1.1); `--help` prints the resolved `ServingConfig` schema (NFR-2). `conf/_schemas.py` gains `ServingConfig` (`registry_dir`, `host`, `port`) and `AppConfig.serving: ServingConfig | None = None` (the `None` default keeps the train CLI and config-smoke surface unchanged). `conf/serving/default.yaml` mirrors schema defaults (Hydra group-directory layout; load via `+serving=default`). 17 integration tests in `tests/integration/serving/test_api.py` covering AC-1..AC-4, D6/D9 default selection, D7 lazy-load cache, D11 log schema, and the six-family parity parametrisation.
- Stage 12: `bristol_ml.models.io.save_skops` / `load_skops` — canonical skops serialisation helpers wrapping `skops.io.dump` / `skops.io.load` with the project trust-list (`_PROJECT_SAFE_TYPES` populated via `register_safe_types`). Atomic write (tmp file + `os.replace`). `load_skops` invokes `skops.io.get_untrusted_types` first and raises `UntrustedTypeError` naming any type outside the project trust-list so operators can distinguish a legitimate new class from a malicious artefact. `UntrustedTypeError` exported from `bristol_ml.models.io`. Any future model family must call `register_safe_types("module.path.ClassName")` at import time for every custom class in its artefact.
- Stage 12: `docs/architecture/layers/serving.md` — new layer doc covering the public surface (`build_app`, `PredictRequest`/`PredictResponse`, `run_id` semantics), training-serving-skew teaching point, the seven-field log contract for Stage 18, the `nn_temporal` warmup-envelope note, the skops security boundary, and the curl example.
- Stage 12: ADR [`0005-skops-for-model-serialisation.md`](docs/architecture/decisions/0005-skops-for-model-serialisation.md) records the joblib → skops migration (D10 Ctrl+G reversal) — threat model, envelope-of-primitives invariant that keeps the trust-list empty today, trust-list contract for future model families, and the one-stage joblib deprecation window. ADR [`0006-serving-lazy-load-cache.md`](docs/architecture/decisions/0006-serving-lazy-load-cache.md) records the Stage 12 D7 *single highest-leverage cut* — lifespan loads only the default model; non-default `run_id`s lazy-load and cache in `app.state.loaded`. ADR slot 0004 remains reserved for the deferred model-dispatcher consolidation refactor (Stage 7/8 housekeeping carry-over).

- Stage 11: `bristol_ml.models.nn.NnTemporalModel` — second PyTorch model family behind the Stage 4 `Model` protocol; a 6-block dilated causal Temporal Convolutional Network (Bai et al. 2018) sized at the CUDA defaults of `seq_len=168`, `num_blocks=8`, `channels=128`, `kernel_size=3`, `dropout=0.2`, `weight_norm=True`, `learning_rate=1e-3`, `batch_size=256`, `max_epochs=100`, `patience=10`, `device="auto"` — all Hydra-overridable via `NnTemporalConfig`. Receptive field at the production spec is `1 + 2·(k−1)·(2^num_blocks − 1) = 1021` steps, ~6× headroom over the weekly cycle (168 h). Causal padding uses `F.pad(x, (left_pad, 0))` *before* `Conv1d(padding=0)` with `left_pad = (k - 1) * dilation` precomputed per block — *not* `Conv1d(padding=...)`, which is symmetric and silently leaks the future. `LayerNorm` is applied over the **channel** dimension (with a `transpose(1, 2)` round-trip), not over time, for the same reason. Each `_TemporalBlockImpl` wraps its two convolutions in `torch.nn.utils.parametrizations.weight_norm` (the modern parametrizations API — `state_dict` keys are `parametrizations.weight.original0` / `original1`, not the legacy `weight_g` / `weight_v`). A 1×1 residual skip handles channel-count mismatches; the `_NnTemporalModuleImpl` head reads the *last* timestep only. Pattern A exogenous handling (plan D3): weather + calendar one-hots are concatenated in-sequence rather than fed through a separate "known-future" branch — informationally equivalent at prediction time when the feature set already contains the target-hour weather forecast (which the Stage 5 feature table does), and strictly simpler. Cold-start per fold (Stage 10 D8 inheritance — `fit()` resets `_module`, `loss_history_`, `_best_epoch`, `_seed_used` at entry); the contiguous 10 %-tail validation slice is preserved unchanged from Stage 10 D9 (Scope Diff `PREMATURE OPTIMISATION` cut: no `seq_len` offset on the val split — early-stopping fidelity vs reported holdout metrics, where the latter is fully separated by `SplitterConfig.gap`). Module CLI `python -m bristol_ml.models.nn.temporal --help` exits 0 with zero side effects (NFR-5 — lazy torch import contract preserved).
- Stage 11: `src/bristol_ml/models/nn/_training.py` (243 lines) — shared training-loop helper, lifted verbatim from Stage 10 `NnMlpModel._run_training_loop` and parameterised on `(module, train_loader, val_loader, optimiser, criterion, device, max_epochs, patience, loss_history, epoch_callback)` returning `(best_state_dict, best_epoch)`. The four-stream seed helper `_seed_four_streams(seed, device)` moved with it. `mlp.py` shrank by ~80 lines as the local `_run_training_loop` and `_seed_four_streams` deleted; `NnMlpModel.fit` and `NnTemporalModel.fit` both import and call `run_training_loop`. Realises the Stage 10 D10 extraction seam — Stage 10 `CLAUDE.md` lines 145–152 explicitly named this file, this function name, and this trigger ("when the second torch-backed model arrives with a hand-rolled loop of its own"). Not a `BaseTorchModel` ABC: the extraction is a function, not a class hierarchy (Stage 10 X7 stays cut). Structural regression guard `test_nn_mlp_fit_still_uses_shared_training_loop_after_extraction` pins the Stage 10 call-site against silent regression to a duplicated loop body; the full Stage 10 `tests/unit/models/test_nn_mlp_*` suite passes unchanged at T1.
- Stage 11: `bristol_ml.models.nn.temporal._SequenceDataset` — private `torch.utils.data.Dataset` subclass producing `(features[i:i+seq_len], target[i+seq_len])` tensor pairs **lazily** at `__getitem__` time. Codebase map §4 calculated the eager-materialisation pattern at ~1.4 GB on the intent's default feature set; lazy holds it at ~10 MB. Built inside `_build_sequence_dataset_class()` for the same `sys.modules`-install pickleability reason as `_NnMlpModuleImpl` (Stage 10 Gotcha 1) — `__module__` and `__qualname__` are patched, then the class is installed onto the module so `pickle` can resolve it by dotted path. `__len__` returns `len(features) - seq_len`; `__init__` does not allocate a tensor larger than the input frame (structural guard `test_sequence_dataset_does_not_eagerly_materialise_full_tensor` against accidentally regressing to the eager pattern).
- Stage 11: `bristol_ml.models.nn.NnTemporalModel.save(path)` / `.load(path)` — single-joblib envelope contract (plan D5) inheriting the Stage 10 D5-revised file-path semantics from the Stage 9 registry. The envelope dict carries the Stage 10 fields **plus one new field `seq_len: int`** — redundant with `config_dump["seq_len"]` but explicit so the load path can sanity-check the window size before reconstructing the module, and so a future migration tool can inspect a stale artefact without round-tripping through a possibly breaking schema. `load(path)` reads the envelope via `load_joblib`, asserts envelope `seq_len` matches `config_dump["seq_len"]`, instantiates `NnTemporalConfig` from `config_dump` (Pydantic re-validation catches schema drift), rebuilds the `_NnTemporalModuleImpl` skeleton with the right `input_dim` / `seq_len` / `num_blocks` / `channels` / `kernel_size` / `dropout` / `weight_norm`, materialises `state_dict` via `torch.load(BytesIO(state_dict_bytes), weights_only=True, map_location="cpu")`, and calls `load_state_dict(strict=True)`. Strict mode catches a mismatch in any Conv1d kernel shape, so a saved 8-block × 128-channel artefact cannot silently load into a 4-block × 32-channel skeleton.
- Stage 11: Pydantic schema `NnTemporalConfig` in `conf/_schemas.py` (15 fields): `type: Literal["nn_temporal"]`, `target_column="nd_mw"`, `feature_columns: list[str] | None = None`, architecture knobs (`seq_len=168`, `num_blocks=8`, `channels=128`, `kernel_size=3`, `dropout=0.2 ∈ [0, 1)`, `weight_norm=True`), optimisation knobs (`learning_rate=1e-3`, `weight_decay=0.0`, `batch_size=256`, `max_epochs=100`, `patience=10`), `seed: int | None = None` (falls back to `project.seed + fold_index` per Stage 10 D8 inheritance), and `device: Literal["auto","cpu","cuda","mps"]="auto"`. A `@model_validator` rejects `seq_len < max(2·kernel_size, receptive_field // 8)` — the validator is one Pydantic hook, kept because silently shipping a TCN whose effective receptive field exceeds the input window is a correctness bug, not polish. `ModelConfig` discriminated union extended to six members: `NaiveConfig | LinearConfig | SarimaxConfig | ScipyParametricConfig | NnMlpConfig | NnTemporalConfig` with `Field(discriminator="type")` on `AppConfig.model`. New Hydra group file `conf/model/nn_temporal.yaml` with `# @package _group_`, `type: nn_temporal`, defaults mirroring the schema, and a header comment naming the CPU recipe (`num_blocks=4 channels=32 batch_size=32 max_epochs=20 device=cpu`).
- Stage 11: `python -m bristol_ml.train model=nn_temporal` end-to-end path — one new `isinstance(model_cfg, NnTemporalConfig)` branch in `src/bristol_ml/train.py::_cli_main` (plan D13 clause ii, mirroring the Stage 10 `NnMlpConfig` cascade) instantiates `NnTemporalModel(model_cfg)` and sets `primary_kind = "nn_temporal"`; `_target_column()` extended to recognise `NnTemporalConfig`. Dispatch through `_dispatch.model_type(model)` resolves `"nn_temporal"` sidecar type once the registry dict entries land.
- Stage 11: `bristol_ml.registry._dispatch` — one new key in each of the two inverse dicts (`_TYPE_TO_CLASS["nn_temporal"] = NnTemporalModel`, `_CLASS_NAME_TO_TYPE["NnTemporalModel"] = "nn_temporal"`). This is the sixth model family the registry dispatches; AC-1 four-verb cap preserved (no new verb). Two symmetry-guard tests added on the inverse pair.
- Stage 11: `bristol_ml.evaluation.harness._build_model_from_config` — **two** new branches in the same T6 commit. The new `isinstance(model_cfg, NnTemporalConfig)` branch (plan D13 clause iii) **and** the missing `isinstance(model_cfg, NnMlpConfig)` branch that Stage 10 shipped without (plan D14 catch-up). The codebase map flagged the Stage 10 gap as latent until someone calls the harness CLI's internal factory with a `NnMlpConfig`; fixing in-commit because T6 already touches the function and a named `Stage 10 catch-up` audit-trail commit message is honest. Local imports keep the harness module lazy with respect to torch.
- Stage 11: `notebooks/11-complex-nn.ipynb` — thin demo notebook (13 cells: 7 markdown + 6 code). Scoped to a per-notebook registry at `data/registry/_stage_11/` (gitignored via `data/*`) so notebook execution does not pollute the human's main registry. CPU-friendly TCN overrides (`seq_len=48 num_blocks=3 channels=16 kernel_size=3 batch_size=64 max_epochs=10`) keep `nbconvert --execute` wall-clock at **17.1 s** on the dev host. Cells: bootstrap repo-root via `pyproject.toml` walk, load the `weather_only` cache and split 80 / 20 on a contiguous tail, populate the registry for the five prior families if missing (the cold-registry guard), fit `NnTemporalModel` with the live loss-curve via `epoch_callback` (plan D6 / AC-3 demo moment), register the run via `registry.save`, build the **ablation table** predict-only over every registered run (six families × four metric columns: `MAE_MW`, `MAPE_pct`, `RMSE_MW`, `WAPE_pct`), and close with the arc commentary and the column-deferral note. The headline result on the same single-holdout slice (~432 rows, last 20 % of the 90-day `weather_only` cache, all six models scored on identical indices): **`nn_temporal` wins on every metric (MAE 3768.6 MW)**, beating the `naive` seasonal baseline (4354.0) by ~13 %, the Stage 10 MLP (5241.1) by ~28 %, the Stage 5 linear (5347.8) by ~30 %, and the Stage 7/8 SARIMAX/scipy_parametric registered runs by an order of magnitude (the latter two underperforming because the predict-only protocol on a 432-row slice exposes their state-space and parametric-form sensitivities — a documented feature of single-holdout ablation against registered runs, called out in the notebook's closing commentary). Generator script at `scripts/_build_notebook_11.py` mirrors the Stage 10 builder pattern (`md` / `code` helpers, per-cell IDs via `_next_id()`, `T7 Cell N` source-text markers); three-step regen is `uv run python scripts/_build_notebook_11.py && jupyter nbconvert --execute --to notebook --inplace notebooks/11-complex-nn.ipynb && ruff format notebooks/11-complex-nn.ipynb`.
- Stage 11: spec-derived test suite — 20 new tests across seven files. `tests/unit/models/test_nn_training_extraction.py` (1 test: structural regression guard that `NnMlpModel.fit` invokes `_training.run_training_loop` exactly once per call). `test_sequence_dataset.py` (3 tests: lazy-window `__len__` / `__getitem__`, eager-materialisation guard on a 50,000-row fixture, dtype contract). `test_nn_temporal_scaffold.py` (3 tests: `Model`-protocol `isinstance`, standalone CLI exit 0, lazy-torch-import contract via `sys.modules` inspection). `test_nn_temporal_fit_predict.py` (9 tests — the AC-1 / AC-2 / NFR-1 core): fit/predict round-trip on a tiny fixture pinning `device="cpu"`, NFR-1 CPU bit-identity via `torch.equal`, NFR-1 CUDA close-match via `torch.allclose(atol=1e-5, rtol=1e-4)` (`@pytest.mark.gpu`, skipped without CUDA), different-seeds-different-state-dicts, `loss_history_` shape + finite floats, `epoch_callback` defensive-copy semantics, cold-start-per-fold, AC-2 shared-loop call-site guard, and **the causal-padding leak guard** — `inf` in every strictly-future position yields finite output (any right-pad leakage produces `inf` / `nan`). `test_nn_temporal_save_load.py` (4 tests: AC-4 `torch.equal` round-trip with `seq_len` byte-exact, missing-file `FileNotFoundError`, single-joblib-file structural guard, pickle round-trip via `_nn_temporal_module_impl_is_pickleable`). `test_registry_nn_temporal_dispatch.py` (3 tests: sixth-family registry round-trip, sidecar `type == "nn_temporal"`, train-CLI isinstance branch source-inspection guard). `tests/integration/test_train_cli_registers_nn_temporal.py` (1 test: full-pipeline `_cli_main` with CPU recipe, asserts exit 0 and a single `Registered run_id:` line). `tests/unit/evaluation/test_harness.py` extended with two D14-catch-up tests (`_build_model_from_config(NnTemporalConfig) → NnTemporalModel`, `_build_model_from_config(NnMlpConfig) → NnMlpModel`). Notebook integration test executes the full `nbconvert --execute` round-trip under `@pytest.mark.slow`. Full suite green.
- Stage 11: `docs/architecture/layers/models-nn.md` — new "Stage 11 addition" section (~150 lines, seven subsections A–G) covering the `NnTemporalModel` contract, `_SequenceDataset` lazy windowing, Pattern A exogenous handling, save-envelope `seq_len` field diff, training-loop ownership migration, D14 harness-factory catch-up, and the ablation-table contract. Module shape diagram updated with `temporal.py` and `_training.py`; module inventory table grew rows for `temporal.py` (Shipped) and flipped `_training.py` from Planning to Shipped. Status flipped from "Provisional" to "Stable for two families". `src/bristol_ml/models/nn/CLAUDE.md` extended with four Stage-11-specific gotchas on top of the five Stage 10 gotchas: causal padding via `F.pad(x, (left, 0))` before `Conv1d(padding=0)` (not `Conv1d(padding=...)`), modern `torch.nn.utils.parametrizations.weight_norm` API state-dict-key shapes (`parametrizations.weight.original0/1` vs legacy `weight_g/weight_v`), `_SequenceDataset` lazy-window contract (do not eagerly materialise), and the new `seq_len: int` field on the single-joblib envelope.
- Stage 11: retrospective at [`docs/lld/stages/11-complex-nn.md`](docs/lld/stages/11-complex-nn.md), including the observed CPU wall-clock data point (**17.1 s** for the full 13-cell `nbconvert --execute`, dominated by the TCN-fit cell — well inside NFR-2's "informational, not a hard gate" 10-minute CPU envelope), the verbatim ablation table (six families × four metric columns), commentary on the SARIMAX / scipy_parametric protocol-mismatch underperformance, and the three documented deviations from plan D10 (four metric columns shipped vs the plan's seven; `to_string` rendering vs `to_markdown`; `NaiveModel(strategy="same_hour_same_weekday")` vs the plan-drafted `same_hour_last_week` which raised on the 432-row holdout). The plan's NFR-2 < 5 min CUDA-defaults wall-clock observation is recorded as a future-host data point — the notebook intentionally runs on the CPU recipe to keep `@pytest.mark.slow` CI honest.

- Stage 10: `bristol_ml.models.nn.NnMlpModel` — first PyTorch model family behind the Stage 4 `Model` protocol. Small MLP (1 hidden layer × 128 units, ReLU, Adam `lr=1e-3`, `batch_size=32`, `max_epochs=100`, `patience=10` — all Hydra-overridable via `NnMlpConfig`). Hand-rolled training loop at `NnMlpModel._run_training_loop` with patience-based early stopping, best-epoch weight restore, a contiguous 10 %-tail internal validation slice (time-series-safe, not a random split — plan D9), cold-start per fold (plan D8 / Ash & Adams NeurIPS 2020), and a `loss_history_: list[dict[str, float]]` attribute populated per epoch with `{"epoch", "train_loss", "val_loss"}`. Optional `epoch_callback: Callable[[dict[str, float]], None]` seam on `fit()` — the notebook's live-loss-curve cell passes a callback that `clear_output(wait=True)` + redraws the figure; the models layer never imports IPython. Z-score normalisation of features and target on per-fold train statistics, with the four scaler tensors (`feature_mean`, `feature_std`, `target_mean`, `target_std`) registered via `register_buffer(...)` so they ride inside `state_dict()` and round-trip cleanly through `torch.save` / `torch.load` (plan D4). Explicit Stage 11 extraction seam flagged in a comment inside `_run_training_loop` (plan D10) — when Stage 11's temporal architecture arrives with a hand-rolled loop of its own, the loop body + `_make_mlp` extract to `src/bristol_ml/models/nn/_training.py` under a shared helper.
- Stage 10: module-level helpers in `bristol_ml.models.nn.mlp` — `_select_device(preference: str) -> torch.device` resolves `"auto"` in order CUDA → MPS → CPU with unit-testable monkeypatched branches (plan D11, human Ctrl+G amendment 2026-04-24 re-opening Scope Diff X5); pinned values (`"cpu"` / `"cuda"` / `"mps"`) honoured verbatim; unknown values raise `ValueError`. `_seed_four_streams(seed, device)` covers `random`, `numpy.random`, `torch.manual_seed`, and `torch.cuda.manual_seed_all` (a no-op on non-CUDA); on CUDA additionally sets `torch.backends.cudnn.deterministic = True` / `torch.backends.cudnn.benchmark = False` (plan D7'). `torch.use_deterministic_algorithms(True)` is **off** — intent AC-2's "within the constraints of non-deterministic GPU operations" carve-out closed OQ-A / H-5 in the negative at the 2026-04-24 Ctrl+G. `_make_mlp(input_dim, config) -> nn.Module` is a module-level factory; the `_NnMlpModule` subclass is module-level (not nested) so joblib round-trip works (plan T2 surprise S2 guard — same discipline `ScipyParametricModel._parametric_fn` required at Stage 8). Lazy torch imports inside function bodies keep `python -m bristol_ml.models.nn.mlp --help` zero-side-effect at ~0 torch import cost.
- Stage 10: `bristol_ml.models.nn.NnMlpModel.save(path)` / `.load(path)` — single-joblib envelope contract (plan D5, revised mid-T3). The Stage 9 registry passes `artefact/model.joblib` as a **file** path (`registry/_fs.py::_atomic_write_run` hard-codes the filename), not a directory, so `save()` writes one joblib file containing `{"state_dict_bytes": torch.save(state_dict, BytesIO).getvalue(), "config_dump": NnMlpConfig.model_dump(), "feature_columns": tuple, "seed_used": int, "best_epoch": int, "loss_history": list[dict], "fit_utc": iso-str, "device_resolved": str}`. `load(path)` reads the envelope via `load_joblib`, rebuilds `NnMlpConfig` from `config_dump` (Pydantic re-validation catches schema drift), instantiates `NnMlpModel(config)`, materialises the `state_dict` via `torch.load(BytesIO(state_dict_bytes), weights_only=True, map_location="cpu")`, and calls `load_state_dict(strict=True)`. `weights_only=True` preserves PyTorch 2.6+'s safety rail on the inner bytes payload; `map_location="cpu"` is load-bearing for the Stage 12 CPU-only serving stub. `test_nn_mlp_save_writes_single_joblib_file_at_given_path` is the structural guard against regressing to the pre-D5-revision two-file layout (`model.pt` + `hyperparameters.json`).
- Stage 10: `bristol_ml.evaluation.plots.loss_curve(history, *, title="Training loss", ax=None) -> Figure` — two-line (train + validation) per-epoch loss plot consuming `NnMlpModel.loss_history_`. Train line uses `OKABE_ITO[1]` (orange), validation `OKABE_ITO[2]` (sky blue) — matches the Stage 6 forecast-overlay convention ("orange is model, blue is truth"). Respects the Stage 6 `ax=` composability contract. Raises `ValueError` on empty history or missing `{"epoch", "train_loss", "val_loss"}` keys. `__all__` and the `plots` CLI's printed-helper list both extended; `evaluation/CLAUDE.md` "Current surface (Plots)" bullet gained a `loss_curve` entry.
- Stage 10: Pydantic schema `NnMlpConfig` in `conf/_schemas.py` (13 fields): `type: Literal["nn_mlp"]`, `target_column="nd_mw"`, `feature_columns: list[str] | None = None` (harness-fallback idiom), architecture knobs (`hidden_sizes=[128]`, `activation: Literal["relu","tanh","gelu"]="relu"`, `dropout: float = 0.0 ∈ [0, 1)`), optimisation knobs (`learning_rate=1e-3`, `weight_decay=0.0`, `batch_size=32`, `max_epochs=100`, `patience=10`), `seed: int | None = None` (falls back to `project.seed + fold_index` per D8), and `device: Literal["auto","cpu","cuda","mps"]="auto"` (D11). `ModelConfig` discriminated union extended to `NaiveConfig | LinearConfig | SarimaxConfig | ScipyParametricConfig | NnMlpConfig` with `Field(discriminator="type")` on `AppConfig.model`. New Hydra group file `conf/model/nn_mlp.yaml` with `# @package _group_`, `type: nn_mlp`, and defaults mirroring the schema.
- Stage 10: `python -m bristol_ml.train model=nn_mlp` end-to-end path — one new `isinstance(model_cfg, NnMlpConfig)` branch in `src/bristol_ml/train.py::_cli_main` (plan D2 clause iii, mirroring the Stage 8 `ScipyParametricConfig` cascade) instantiates `NnMlpModel(model_cfg)` and sets `primary_kind = "nn_mlp"`; the Stage 9 `evaluate_and_keep_final_model` → `registry.save` pipeline handles the rest unchanged. Dispatch through `_dispatch.model_type(model)` resolves `"nn_mlp"` sidecar type automatically once the dict entry lands (below). The registry leaderboard is how `nn_mlp` competes against the other families at Stage 10; the hard-wired `benchmarks.compare_on_holdout` three-way chart is **not** touched (plan D2 clause iv — NN is out of the NESO three-way benchmark at Stage 10, in line with the Stage 4 / Stage 6 harness API-growth rule).
- Stage 10: `bristol_ml.registry._dispatch` — one new key in each of the two inverse dicts (`_TYPE_TO_CLASS["nn_mlp"] = NnMlpModel`, `_CLASS_NAME_TO_TYPE["NnMlpModel"] = "nn_mlp"`). This is the fifth model family the registry dispatches; AC-1 four-verb cap preserved (no new verb). Structural test `test_registry_save_nn_mlp_model_via_protocol` extends the Stage 9 AC-2 test suite with a fifth-family save + load round-trip at `torch.allclose(atol=1e-10)` on predict output.
- Stage 10: `notebooks/10-simple-nn.ipynb` — thin demo notebook (11 cells). Bootstrap + cache load of the last 60 days of the `weather_only` feature table → live-demo fit invoking `NnMlpModel.fit(..., epoch_callback=on_epoch)` with the live-plot cell owning `IPython.display.display(handle, display_id=True)` + `clear_output(wait=True)` (AC-3 Demo moment; plan D6 / §5) → static `plots.loss_curve(model.loss_history_)` render (AC-3 static half) → three-way `harness.evaluate` comparison (naive + linear + nn_mlp) on a narrow rolling-origin window → closing Stage 11 forward pointer. Pins `device="cpu"` end-to-end for CI determinism. Generator script at `scripts/_build_notebook_10.py` follows the Stage 7 / Stage 8 `md` / `code` helper pattern with per-cell IDs via `_next_id()`; three-step regen is `uv run python scripts/_build_notebook_10.py && jupyter nbconvert --execute --to notebook --inplace notebooks/10-simple-nn.ipynb && ruff format notebooks/10-simple-nn.ipynb`.
- Stage 10: spec-derived test suite — 31 tests across six files. `test_nn_mlp_scaffold.py` (4 tests: `Model`-protocol `isinstance`, config-defaults Hydra round-trip, standalone CLI exits 0, module docstring non-empty). `test_nn_mlp_fit_predict.py` (19 tests — the AC-2 / AC-3 core): fit/predict round-trip on a tiny fixture pinning `device="cpu"`, AC-2 CPU bit-identity via `torch.equal` on every parameter tensor + `predict` output, AC-2 `@pytest.mark.gpu` close-match on CUDA via `torch.allclose(atol=1e-5, rtol=1e-4)` (skipped when `torch.cuda.is_available()` is false), different-seeds-different-state-dicts regression guard, `loss_history_` shape + finite-floats contract, `epoch_callback` invocation, early-stopping-before-max-epochs-on-plateau with best-epoch restore, cold-start-per-fold, three `_select_device` branches via monkeypatched `torch.cuda.is_available` / `torch.backends.mps.is_available`, `device="cpu"`-on-CUDA-host honoured, invalid-device-raises-ValueError, and a structural guard that the val tail is a contiguous slice from the end of the train window. `test_nn_mlp_save_load.py` (5 tests): the AC-4 `torch.allclose(atol=1e-10)` round-trip, missing-file raises `FileNotFoundError`, the pre-D5-revision two-file-layout regression guard, unfitted-model-rejects, and Pydantic schema-drift catch via `config_dump` re-validation. `test_registry_nn_mlp_dispatch.py` (3 tests): fifth-family registry round-trip, sidecar `type == "nn_mlp"`, and a source-inspection guard that `train.py::_cli_main` contains the `isinstance(model_cfg, NnMlpConfig)` branch. `test_plots_loss_curve.py` (4 tests): figure render from history, `ax=` composability, empty-history rejection, missing-required-key rejection. `tests/integration/test_train_cli_registers_nn_mlp.py` (1 test): full-pipeline `_cli_main` with `model=nn_mlp model.hidden_sizes=[4] model.max_epochs=3 model.batch_size=64 model.device=cpu`, asserts exit 0, per-fold banner, `Registered run_id:` line, exactly one run, sidecar `type == "nn_mlp"`. Full suite: **644 passed, 5 deselected** (4 `slow`, 1 `gpu`).
- Stage 10: `torch>=2.7,<3` added to `[project].dependencies` in `pyproject.toml`. New `[tool.uv.sources]` + `[[tool.uv.index]] name = "pytorch-cu128"` stanza pinning `torch` to the `https://download.pytorch.org/whl/cu128` wheel index under `marker = "sys_platform == 'linux'"` (Blackwell / sm_120 support — cu128 is the first PyTorch wheel track with stable sm_120, hence the bump from the research-draft's `>=2.3`) and to PyPI on other platforms (MPS wheels ship on PyPI for macOS). Dockerfile **not** touched — the mid-T2 D1 amendment (2026-04-24) dropped the pre-warm clause; the persistent `.venv` mount on `/workspace` means a one-time `uv sync` per container image brings torch into the venv and subsequent sessions reuse it. New `gpu` pytest marker registered in `[tool.pytest.ini_options].markers`; default `addopts` extended from `-m 'not slow'` to `-m 'not slow and not gpu'` so CI stays CPU-only by default. `uv.lock` regenerated.
- Stage 10: `docs/architecture/layers/models-nn.md` — new sub-layer doc (~400 lines) covering module layout, the five-site dispatch story, the public interface of `NnMlpModel`, the D7' device-split reproducibility contract, the D5 single-joblib envelope with full field annotation, D8 cold-start, D9 10 %-tail early stopping + best-epoch restore, D11 device auto-select, the Stage 11 extraction seam (D10), and an explicit "not shipped" note enumerating the X5 / X6 / X7 Scope Diff deferrals. Parent `docs/architecture/layers/models.md` remains authoritative for the `Model` protocol + `ModelMetadata` + joblib IO shared by every family. `src/bristol_ml/models/nn/CLAUDE.md` — module-local guide naming the five PyTorch-specific gotchas (module-level `_NnMlpModule` for joblib pickleability, lazy torch imports, buffer registration timing for strict-mode round-trip, `weights_only=True + map_location="cpu"`, single-joblib envelope). `docs/architecture/README.md` module catalogue extended with a `models/nn/` row (H-3 / Scope diff X3).
- Stage 10: retrospective at [`docs/lld/stages/10-simple-nn.md`](docs/lld/stages/10-simple-nn.md), including the observed CPU wall-clock data point (**18.3 s** for the six-fold rolling-origin harness on the `weather_only` feature table, 22.9 s end-to-end including torch import; ~10× headroom over AC-5's "reasonable time on a laptop CPU" envelope). NFR-2 (3-min wall-clock ceiling as a regression gate) cut per the Scope Diff — the observation is an informational data point, not a gate.

- Stage 9: `bristol_ml.registry` — filesystem-backed model registry with a four-verb public surface `save(model, metrics_df, *, feature_set, target, registry_dir=None) -> run_id`, `load(run_id, *, registry_dir=None) -> Model`, `list_runs(*, target=None, model_type=None, feature_set=None, sort_by="mae", ascending=True, registry_dir=None) -> list[dict]`, `describe(run_id, *, registry_dir=None) -> dict`. Exported as `__all__ = ("save", "load", "list_runs", "describe")` — AC-1 four-verb cap enforced structurally by `test_registry_public_surface_does_not_exceed_four_callables`. `list_runs` (rather than `list`) avoids shadowing the Python builtin; CLI and layer-doc verb are both "list". On-disk layout `data/registry/{run_id}/{artefact/model.joblib, run.json}` with `run_id = {metadata.name}_{YYYYMMDDTHHMM}` (minute-precision UTC; plan D3, human amendment). Atomic save via temp-dir-then-`os.replace` mirroring the ingestion layer's `_atomic_write` idiom (plan D5). Sidecar JSON schema pinned via `SidecarFields` TypedDict with `json.dumps(..., indent=2, allow_nan=True, ensure_ascii=False)` — `allow_nan=True` preserves `ScipyParametricModel`'s `float("inf")` covariance entries (plan D4 + R3). Last-write-wins on same-minute run-id collisions (plan D2 + R7); single-author pace makes collisions vanishingly rare. `git_sha` auto-captured at save time via `_git.py::_git_sha_or_none()` (plan D13 / AC-3); the four model classes are unchanged (AC-2). Registry-local type dispatcher at `_dispatch.py` (plan H4 — not a third shared dispatcher site); `_NamedLinearModel` maps to `"linear"` by class name so `load()` returns a base `LinearModel` (plan D16 cut: dynamic name is preserved on the sidecar but not re-applied to the loaded instance). `DEFAULT_REGISTRY_DIR = Path("data/registry")` — gitignored via the repo-wide `data/*` rule; no new `.gitignore` entry required. No `RegistryConfig` Pydantic schema, no Hydra config group, no `AppConfig` change (plan D17 cut).
- Stage 9: `python -m bristol_ml.registry` — `argparse`-based CLI (plan D16, same idiom as `train.py`) with two subcommands: `list` prints a plain-text leaderboard sorted by `--sort-by` (default `mae`, ascending) and filterable by `--target` / `--model-type` / `--feature-set` (plan D7); `describe <run_id>` pretty-prints one sidecar as indented JSON. `--registry-dir` flag on both subcommands for tests and throwaway demos. Leaderboard rendering uses plain `str.format` padding — no `tabulate` dependency.
- Stage 9: `bristol_ml.evaluation.harness.evaluate_and_keep_final_model(model, df, splitter_cfg, metrics, *, target_column="nd_mw", feature_columns=None) -> tuple[pd.DataFrame, Model]` — new public function returning both the per-fold metrics DataFrame and the final-fold fitted model handle. Respects the evaluation-layer H5 API-growth rule (plan D17 + `evaluation/CLAUDE.md`): no second boolean flag on `evaluate()`. Internally delegates to `evaluate(...)` so the metric-computation contract stays at exactly one function.
- Stage 9: `bristol_ml.train` — two new CLI flags: `--registry-dir PATH` overrides the registry root for tests and facilitator demos, and `--no-register` skips the `registry.save(...)` step entirely for notebook-style workflows. When the flag is absent the CLI registers the final-fold model via `evaluate_and_keep_final_model` (plan D17 — no re-fit on full data; the leaderboard metric is the rolling-origin cross-fold summary, and the artefact is an honest representative of that measurement). Prints `Registered run_id: {run_id}` on success.
- Stage 9: spec-derived test suite at `tests/unit/registry/` — 51 tests across five files. `test_registry_fs.py`: 10 T1 scaffold tests covering the run-ID minute-precision regex, UTC-naive rejection, git-SHA helper behaviour inside/outside a git tree, `_atomic_write_run` layout + last-write-wins + staging-cleanup-on-error, and the AC-1 structural cap on `__all__`. `test_registry_save_load.py`: the four AC-2 save-and-round-trip tests across `NaiveModel` / `LinearModel` / `SarimaxModel` / `ScipyParametricModel` (`atol=1e-10`), the two AC-3 tests (automatic git-SHA capture + missing-explicit-kwarg rejection), plus unfitted-model and `_NamedLinearModel` guards. `test_registry_list_describe.py`: 12 T4 tests covering the AC-4 `< 1 s` performance gate on 100 runs, the three D7 filter dimensions, four D8 sort scenarios (default, descending, alternative metric, missing-metric-sorts-last), staging-directory skip, and empty-registry / missing-run paths. `test_registry_cli.py`: 9 T5 tests covering CLI `list` / `describe` / `--help` via subprocess, the new `evaluate_and_keep_final_model` contract, and the `train`-CLI registration path including the `--no-register` flag. `test_registry_layer_doc.py`: the two AC-5 gates (`test_registry_layout_documentation_exists` and `test_registry_run_json_is_hand_parseable`). Integration test at `tests/integration/test_registry_mlflow_adapter.py` — the D10 MLflow PyFunc round-trip (`pytest.importorskip`-gated for lean installs).
- Stage 9: `docs/architecture/layers/registry.md` — new layer doc covering on-disk layout, sidecar JSON schema (field-by-field), the four-verb contract, atomic-save lifecycle, VCS policy, the "Graduation to MLflow" subsection documenting the PyFunc adapter pattern and the test-only proof, the joblib / `skops.io` deferral note pointing at Stage 12 (D14 / H-3), NFR gates, upgrade seams, and a module inventory (plan D18).
- Stage 9 (D10): test-only `mlflow.pyfunc.PythonModel` adapter at `tests/integration/mlflow_adapter.py` — `RegistryPyfuncAdapter` subclass delegates to `registry.load()` via `context.artifacts["registry_run"]` in `load_context`; `package_run_as_pyfunc(run_id, dst, *, registry_dir)` helper calls `mlflow.pyfunc.save_model` with the registry run directory as the artefact. Integration test `test_registry_run_is_loadable_via_mlflow_pyfunc_adapter` round-trips a registered `NaiveModel` through `mlflow.pyfunc.save_model` → `mlflow.pyfunc.load_model` → `predict` and asserts `numpy.allclose(..., atol=1e-10)` against the wrapped model's output. Proves the layer doc's "mechanical migration to a hosted MLflow registry" claim is falsifiable; a future PyFunc-API break in MLflow 3.x lands as a red test at the version-bump boundary rather than silently in production. Upgraded from plan D10's original "document the adapter" to "document AND test the adapter" at human Ctrl+G markup 2026-04-23.
- Stage 9: `mlflow>=2.14,<3` added to the `dev` dependency group in `pyproject.toml` — **test-only**; MLflow is not a runtime dependency and `bristol_ml.registry` does not import it. `uv.lock` regenerated. `pytest.importorskip("mlflow")` guards the adapter test so lean installs without `--group dev` still pass the rest of the suite.
- Stage 9: retrospective at [`docs/lld/stages/09-model-registry.md`](docs/lld/stages/09-model-registry.md); [`docs/architecture/layers/models.md`](docs/architecture/layers/models.md) updated per housekeeping H-3 (`skops.io` upgrade-seam note moved from Stage 9 to Stage 12); [`src/bristol_ml/models/io.py`](src/bristol_ml/models/io.py) module docstring updated to name Stage 12 as the `skops.io` trigger (plan D14); [`src/bristol_ml/models/CLAUDE.md`](src/bristol_ml/models/CLAUDE.md) security-note paragraph re-worded to reflect Stage 12 as the inflection point.

- Stage 8: `bristol_ml.models.scipy_parametric.ScipyParametricModel` — `scipy.optimize.curve_fit` parametric load model behind the Stage 4 `Model` protocol. The functional form is `α + β_heat · HDD + β_cool · CDD + Σ_k (A_k sin + B_k cos)(diurnal) + Σ_j (C_j sin + D_j cos)(weekly)` with Elexon-convention fixed hinges `T_heat = 15.5 °C`, `T_cool = 22.0 °C` (plan D1); `HDD = max(0, T_heat − T)`, `CDD = max(0, T − T_cool)`. Default harmonic counts are `diurnal_harmonics=3` (period 24 h) + `weekly_harmonics=2` (period 168 h), giving 13 free parameters (plan D2). `curve_fit(method="lm", loss="linear", maxfev=5000)` by default (plan D3 + D6) so the covariance-derived 95 % CIs stay rigorously Gaussian; `loss ∈ {"soft_l1", "huber", "cauchy"}` available as CLI override. `p0` derived deterministically from training data inside `fit()` (plan D4 — `α₀ = target.mean()`, `β_heat₀` from sub-10 °C vs above-20 °C mean difference, `β_cool₀` from above-22 °C vs at-17 °C, Fourier coefficients zero) so rolling-origin folds with seasonal training windows do not land far from any reasonable value. `_parametric_fn`, `_derive_p0`, `_build_param_names` are module-level pure functions (plan T2 surprise S2 guard — required for joblib pickle round-trip). `fit` + `predict` both call a `_require_utc_datetimeindex(features, method=...)` static guard mirroring `SarimaxModel` (plan D8). NFR-4 guard emits a loguru WARNING when `pcov` contains `inf` and stores `float("inf")` in the metadata rather than silently producing vacuous CIs (AC-6). Module CLI `python -m bristol_ml.models.scipy_parametric --help` prints config defaults + curve_fit pointer + Stage 6 palette notice (AC-10).
- Stage 8: Pydantic schemas `ScipyParametricConfig` in `conf/_schemas.py`. `type: Literal["scipy_parametric"]`; `target_column="nd_mw"`, `feature_columns: tuple[str, ...] | None = None`, `diurnal_harmonics=3` (`ge=0, le=10`), `weekly_harmonics=2` (same bound), `t_heat_celsius=15.5`, `t_cool_celsius=22.0`, `loss: Literal["linear","soft_l1","huber","cauchy"] = "linear"`, `max_iter=5000 (ge=1)`, `p0: tuple[float, ...] | None = None`. `ModelConfig` discriminated union extended to `NaiveConfig | LinearConfig | SarimaxConfig | ScipyParametricConfig` with `Field(discriminator="type")` on `AppConfig.model`. New Hydra group file `conf/model/scipy_parametric.yaml` with `# @package model`, `type: scipy_parametric`, and defaults mirroring the schema. **Semantic note (plan D2 clarification):** `ScipyParametricConfig.feature_columns` constrains *Fourier* column names (subset of generated `diurnal_*` / `weekly_*` columns), NOT the raw input columns — unlike `LinearConfig` / `SarimaxConfig`. The temperature column is always implicit; raw column selection happens externally via `harness.evaluate(..., feature_columns=...)`.
- Stage 8: `notebooks/08_scipy_parametric.ipynb` — thin demo notebook (14 cells) covering Hydra config load with `model=scipy_parametric features=weather_calendar` and per-field rolling-origin overrides (`min_train_periods=720, step=1344, test_len=168`), feature-cache load, raw `temperature_2m` vs `nd_mw` scatter (`OKABE_ITO[1]`, α=0.15), single-fold `%%time` fit + print of `popt` / `param_std_errors` / 95 % CIs (AC-4 evidence — Cell 5), pandas parameter-table split by physics vs Fourier columns with `value ± 1.96·σ` (AC-3 "parameter estimates + CIs" — Cell 7), fitted temperature-response curve overlaid on scatter (Fourier rows zeroed via `_parametric_fn` evaluation — Cell 8), rolling-origin `evaluate()` four-way across Naive / Linear / SARIMAX / ScipyParametric (Cell 9), `forecast_overlay` four-way on a shared 48-hour window (AC-3 "forecast comparison" — Cell 10), parameter-stability diagnostic via `rolling_origin_split_from_config` (Cell 11), **Assumptions appendix** spelling out the three Gaussian-CI assumptions — homoscedasticity (GB demand residuals are visibly peak-hour-heteroscedastic), near-linearity around the optimum (weak at hinge transitions), no parameter at a bound — and naming Stage 10 as the owner of bootstrap / quantile alternatives (plan D5 clarification — Cell 12), and closing Stage 9/10 forward pointers (Cell 13). ScipyParametric won the four-way comparison on the seeded synthetic test at T7 time: MAE **223.1 MW** vs SARIMAX 419.6 / Naive 475.6 / Linear 1272.7. Generator script at `scripts/_build_notebook_08.py` follows the Stage 7 pattern (`md` / `code` helpers, per-cell IDs via `_next_id()`) — 3-step regen: build → `jupyter nbconvert --execute` → `ruff format` (plan D9).
- Stage 8: spec-derived test suite — `tests/unit/models/test_scipy_parametric.py` (26 tests covering T1 module-level-function contracts, T2 `_parametric_fn` pickleability / `_derive_p0` finiteness on empty-cooling-segment / `_build_param_names` arity, T3 metadata + unfitted-name regex + CLI-main + UTC-tz guard, T4 fit / predict / re-entrancy / AC-9 deterministic `p0` / AC-6 singular-covariance WARNING / tolerance-recovery / `@slow` fit-time budget, T5 save-unfitted-raises / AC-2 round-trip-predict-equal / AC-5 covariance-bit-exact / wrong-type-load, T7 **AC-4 5 %-recovery on 30-day seasonal sinusoid** (non-harmonic temperature breaks Fourier collinearity for `β_heat`) + `@slow` four-way competitive check on 168-row test windows + AC-1 full-protocol conformance). `tests/integration/test_notebook_08.py` (`@pytest.mark.slow`, AC-3) — `jupyter nbconvert --execute` on a `.pytest-exec.ipynb` sibling copy under `notebooks/` (tmp_path copies defeat the notebook's Cell 1 `pyproject.toml`-walk bootstrap); asserts markers `T5 Cell 5`, `T5 Cell 7`, `T5 Cell 10` each produce non-empty outputs. `tests/unit/test_config.py` (5 new tests pinning `ScipyParametricConfig` defaults, `extra="forbid"`, invalid `loss`, negative harmonics, and discriminated-union dispatch). `tests/unit/evaluation/test_harness.py` (+2: `_build_model_from_config(ScipyParametricConfig) → ScipyParametricModel` + CLI smoke). `tests/unit/test_train_cli.py` (+1: train-CLI branch smoke `model=scipy_parametric`). Full suite green: **549 passed, 4 deselected** (fast); the slow-marked tests pass explicitly under `-m slow`.
- Stage 8: `scipy>=1.13,<2` promoted to `[project].dependencies` in `pyproject.toml` (plan D12). `scipy` was transitively available via `statsmodels` at the Stage 7 baseline but never declared — a fresh Python 3.12 venv would fail at `import scipy.optimize` on a hypothetical clean install that excludes statsmodels. `1.13` is the first release with formal NumPy 2.0 support; `<2` guards a hypothetical ABI break. `uv.lock` regenerated.
- Stage 8: retrospective at [`docs/lld/stages/08-scipy-parametric.md`](docs/lld/stages/08-scipy-parametric.md); [`docs/architecture/layers/models.md`](docs/architecture/layers/models.md) inventory row for `models/scipy_parametric.py` flipped to `Shipped`; Stage 7's "Per-model CLI parity" open question re-deferred per housekeeping H-3 with a back-reference to the Stage 8 plan; [`src/bristol_ml/models/CLAUDE.md`](src/bristol_ml/models/CLAUDE.md) `ScipyParametricModel` subsection finalised with the module-level-function requirement (plan S2), the `pcov`-inf WARNING contract (NFR-4), and the D2 feature-columns-Fourier-semantics note.

- Stage 7: `bristol_ml.models.sarimax.SarimaxModel` — `statsmodels` SARIMAX behind the Stage 4 `Model` protocol. Dual-seasonality via Dynamic Harmonic Regression (Hyndman fpp3 §12.1): `seasonal_order=(1,1,1,24)` handles the daily cycle inside SARIMAX; the weekly period (168 h) is absorbed via three Fourier exogenous regressor pairs appended by `features.fourier.append_weekly_fourier`. A `seasonal_order[3]=168` SARIMAX is deliberately *not* shipped — numerically unstable and breaks the AC-3 fit-time budget (plan D1, research §R2). Constructor defaults honour the statsmodels-maintainer-blessed settings for seasonal demand data (plan D6): `enforce_stationarity=False`, `enforce_invertibility=False`, `concentrate_scale=True`, `simple_differencing=False`, `hamilton_representation=False`, `freq="h"`. `predict(features)` re-indexes the `SARIMAXResults.get_forecast().predicted_mean` output to `features.index` (plan D11 — surprise-1 fix; `test_sarimax_predict_returns_series_indexed_to_features_index` is the load-bearing regression guard). `fit` sets `endog_index.freq = "h"` on a copy of the features index before constructing the endog Series, because parquet round-trips strip `df.index.freq` and the constructor's `freq` kwarg alone is insufficient (`_init_dates` checks `endog.index.freq` first; plan surprise-2). Re-fit per fold in the rolling-origin harness — `apply(refit=False)` deliberately avoided for cross-model comparison parity (plan D5). Module CLI `python -m bristol_ml.models.sarimax --help`.
- Stage 7: `bristol_ml.features.fourier.append_weekly_fourier` — pure, UTC-referenced Fourier-harmonic feature helper. `append_weekly_fourier(df, *, period_hours=168, harmonics=3, column_prefix="week") -> pd.DataFrame` appends `2 * harmonics` columns (`week_sin_k1..kN`, `week_cos_k1..kN`). Converts a tz-aware `DatetimeIndex` to integer hours via `df.index.view("int64") // 3_600_000_000_000` so DST transitions do not introduce phase drift. Returns a new frame (no input mutation, pandas copy-on-write conforming). `harmonics=0` is a no-op fast path; tz-naive input raises `ValueError`. Module CLI `python -m bristol_ml.features.fourier --help`.
- Stage 7: Pydantic schemas `SarimaxConfig` and nested `SarimaxKwargs` in `conf/_schemas.py`. `SarimaxConfig.type: Literal["sarimax"]`; `order=(1,0,1)`, `seasonal_order=(1,1,1,24)`, `weekly_fourier_harmonics=3`, `target_column="nd_mw"`, `feature_columns: tuple[str, ...] | None = None`, `trend: str | None = None`. `SarimaxKwargs` is `frozen=True, extra="forbid"` carrying the five `enforce_*` / `concentrate_scale` / representation flags. `ModelConfig` discriminated union extended to `NaiveConfig | LinearConfig | SarimaxConfig` with `Field(discriminator="type")` on `AppConfig.model`. New Hydra group file `conf/model/sarimax.yaml` with `# @package model`, `type: sarimax`, and defaults mirroring the schema.
- Stage 7: `notebooks/07_sarimax.ipynb` — thin demo notebook (13 cells) covering Hydra config load with per-field rolling-origin overrides (`fixed_window=true, min_train_periods=720, step=1344, test_len=168`), feature-cache load, `nd_mw` seasonal decomposition, ADF stationarity readout after seasonal differencing at lag 24, small AIC sweep (`p,q ∈ {0,1}`, `P,Q ∈ {0,1}`), single-fold `SarimaxModel.fit` + `results.plot_diagnostics(figsize=(14,10))` + `results.summary()`, three-way rolling-origin evaluation (Naive / Linear / SARIMAX) on 6 folds × 168 rows (1008 predictions — sufficient for ACF at lag 168), the **Stage 6 → Stage 7 ACF narrative payoff cell** feeding SARIMAX residuals into `plots.acf_residuals` alongside the linear residuals to show the lag-168 spike is materially flatter, three-way `forecast_overlay` on a shared 48-hour window, and a closing markdown cell with Stage 8 / Stage 10 forward pointers. SARIMAX won on all four metrics at the demo window: MAE 1730 MW vs 1955 (linear) vs 2080 (naive). Executes in ~190 s, well inside the AC-3 10-minute ceiling. Generator script at `scripts/_build_notebook_07.py` is a Python-level JSON authoring tool (md/code helpers, per-cell IDs via `_next_id()`) committed alongside the notebook for deterministic re-generation.
- Stage 7: spec-derived test suite — `tests/unit/features/test_fourier.py` (7 tests: column-name contract, deterministic values at a known timestamp, 168-period identity, `harmonics=0` no-op, tz-naive rejection, non-mutation, DST-transition continuity), `tests/unit/models/test_sarimax.py` extended (protocol conformance across all five `Model` members, fit/predict/save/load round-trip with exog — issue #6542 guard, re-entrant `fit`, predict-before-fit, metadata population, residual-ACF at lag 168 materially lower (≥70%) for SARIMAX than for the linear baseline on a synthetic AR(1) + daily + weekly sine fixture, freq-warning guard pinning that no `ValueWarning` / `UserWarning` fires during `fit`), `tests/unit/test_config.py` extensions (`SarimaxKwargs` defaults match plan D6, `SarimaxConfig` order defaults match D2, `weekly_fourier_harmonics` default is 3, discriminated-union dispatch on `type: "sarimax"`, Hydra `model=sarimax` round-trip, `evaluation.rolling_origin.fixed_window=true step=168` override round-trip), `tests/unit/evaluation/test_harness.py` extensions (`_build_model_from_config` dispatches `SarimaxConfig → SarimaxModel`, end-to-end harness CLI smoke with `model=sarimax`), `tests/unit/test_train_cli.py` extensions (inline-dispatcher smoke for the train CLI). Full suite: 515 passed.
- Stage 7: retrospective at [`docs/lld/stages/07-sarimax.md`](docs/lld/stages/07-sarimax.md); [`docs/architecture/layers/models.md`](docs/architecture/layers/models.md) module inventory row for `models/sarimax.py` flipped to `Shipped`; the models-layer "Per-model CLI parity" open question re-deferred per housekeeping H-3 with a back-reference to the Stage 7 plan; [`src/bristol_ml/models/CLAUDE.md`](src/bristol_ml/models/CLAUDE.md) SARIMAX specifics subsection finalised (freq="h" mandate, predict re-indexing contract, D5 apply-refit pointer).

- Stage 6: `bristol_ml.evaluation.plots` — seven diagnostic-plot helpers for the evaluation layer. Four residual diagnostics (`residuals_vs_time`, `predicted_vs_actual`, `acf_residuals`, `error_heatmap_hour_weekday`), a forecast overlay (`forecast_overlay`), an empirical-quantile uncertainty-band overlay (`forecast_overlay_with_band`), and the fixed-window NESO retrospective bar chart (`benchmark_holdout_bar`) that consumes `NesoBenchmarkConfig.holdout_start/_end` (resolving the Stage 4 carry-over). All helpers are model-agnostic (inputs are `pd.Series` / `pd.DataFrame`, never a `Model` object), accept an optional `ax=` for composability, and inherit their default figsize (12×8) from `PlotsConfig`. Okabe-Ito qualitative palette (Wong 2011 *Nature Methods*), `cividis` sequential colormap, and `RdBu_r` diverging colormap are injected into `plt.rcParams` at module import; `tab10` (matplotlib's default) is explicitly rejected as not CVD-safe. Module CLI `python -m bristol_ml.evaluation.plots --help` prints the exported helper surface, the active palette, and the rcParams written at import time.
- Stage 6: `bristol_ml.evaluation.harness.evaluate(..., return_predictions=True)` — backward-compatible one-off tolerance (plan D9) that returns a `(metrics_df, predictions_df)` tuple with one row per (fold, forecast-horizon-index) in `predictions_df`. Columns pinned: `["fold_index", "test_start", "test_end", "horizon_h", "y_true", "y_pred", "error"]`. Stage 4 call sites are unaffected (default `return_predictions=False` keeps the single-DataFrame return). `@overload` stubs narrow the return type under `Literal[True]` / `Literal[False]`. An **architectural-debt note** in `src/bristol_ml/evaluation/CLAUDE.md` records this as a single-flag concession — the second boolean extension to `evaluate()` triggers a promotion to a first-class `EvaluationResult` dataclass or a typed `evaluate_v2`.
- Stage 6: `matplotlib>=3.8,<4` and `seaborn>=0.13,<1` promoted from `[dependency-groups].dev` to `[project].dependencies` in `pyproject.toml` so `python -m bristol_ml.evaluation.plots --help` works on a non-dev install (plan D3). `uv.lock` regenerated.
- Stage 6: Hydra config group `conf/evaluation/plots.yaml` with user-tweakable defaults (`figsize: [12.0, 8.0]`, `dpi: 110`, `display_tz: "Europe/London"`, `acf_default_lags: 168`). Pydantic `PlotsConfig` in `conf/_schemas.py`; `EvaluationGroup.plots: PlotsConfig` non-optional. Helper-function defaults match config defaults so construction without Hydra still works.
- Stage 6: `notebooks/04_linear_baseline.ipynb` — six Stage 6 appendix cells (indices 13–18) appended after the Stage 4 shipping view. Cells 0–12 (including the inline-matplotlib Cell 11) are byte-identical to Stage 4 per the 2026-04-20 D11 human mandate; the appendix demonstrates the four hero diagnostics in a 2×2 grid, the `forecast_overlay` helper, the uncertainty-band overlay exercising `evaluate(return_predictions=True)`, and the `benchmark_holdout_bar` (guarded on a warm NESO cache). All appendix cells carry a `stage6-appendix` metadata tag so the scratch append is idempotent. Runs end-to-end via `jupyter nbconvert --execute` inside the 180 s budget.
- Stage 6: spec-derived test suite — 41 `test_plots.py` tests (palette-injection, `ax=` composability, `_require_tz_aware_datetime_index` guard, ACF marker placement, DST spring-forward / fall-back gates, `forecast_overlay_with_band` quantile arithmetic, `benchmark_holdout_bar` lazy-import monkeypatch path, model-agnostic signature contract) and 6 `test_harness.py` additions pinning the D9 predictions emission (column order, dtypes, `horizon_h` zero-based per fold, `error = y_true - y_pred` identity). Full suite green: 460 passed.
- Stage 6: retrospective at [`docs/lld/stages/06-enhanced-evaluation.md`](docs/lld/stages/06-enhanced-evaluation.md); [`docs/architecture/layers/evaluation.md`](docs/architecture/layers/evaluation.md) module inventory extended with `plots.py`; the Stage 4 `NesoBenchmarkConfig.holdout_start/_end` open question marked resolved (H-4 from the Stage 6 plan housekeeping).
- Stage 6 (Phase 3 review response): `bristol_ml.evaluation.plots.apply_plots_config(config: PlotsConfig) -> None` — public entry point that re-applies the rcParams overlay with `figure.figsize` and `figure.dpi` sourced from a loaded `PlotsConfig`. Resolves the spec-drift caught at Phase 3 review (N2): D5's "Hydra-configurable figsize / dpi" knobs were decorative — `plots.py` never read `PlotsConfig` at runtime, so `evaluation.plots.figsize=[16,10]` overrides had no effect. Now wired: `apply_plots_config(cfg.evaluation.plots)` propagates overrides to matplotlib.

- Stage 5: `bristol_ml.ingestion.holidays` — GB bank-holidays ingestion from `https://www.gov.uk/bank-holidays.json` under the UK Open Government Licence v3.0. Public interface `fetch(config, *, cache=CachePolicy.AUTO) -> Path` and `load(path) -> pd.DataFrame` targeting a consolidated parquet across all three UK divisions (`england-and-wales`, `scotland`, `northern-ireland`). On-disk schema (`date`, `division`, `title`, `notes`, `bunting`, `retrieved_at_utc`) documented in [`src/bristol_ml/ingestion/CLAUDE.md`](src/bristol_ml/ingestion/CLAUDE.md); duplicate `(date, division)` keys raise as upstream drift. Module CLI `python -m bristol_ml.ingestion.holidays [--cache auto|refresh|offline]`. Cassette at `tests/fixtures/holidays/cassettes/holidays_refresh.yaml`.
- Stage 5: `bristol_ml.features.calendar` — pure derivation of the 44 calendar columns for the `weather_calendar` feature set. `derive_calendar(df, holidays_df) -> pd.DataFrame` appends the 44 `int8` columns described by `CALENDAR_VARIABLE_COLUMNS` (23 hour-of-day one-hots from the UTC hour per human mandate 2026-04-20, 6 day-of-week one-hots and 11 month one-hots both from `Europe/London` local components per D-4 / D-7, 4 holiday flags `is_bank_holiday_ew` / `is_bank_holiday_sco` / `is_day_before_holiday` / `is_day_after_holiday`). `is_weekend` is deliberately not emitted — perfect collinearity with the day-of-week one-hots per external research R5, pinned by a module-level assertion and a runtime guard. Pre-window rows are zero-filled with a single `loguru` WARNING per call (D-6). Module CLI `python -m bristol_ml.features.calendar [--rows N]`.
- Stage 5: `bristol_ml.features.assembler` extended — `CALENDAR_VARIABLE_COLUMNS` re-exported from `features.calendar`; new `CALENDAR_OUTPUT_SCHEMA: pa.Schema` (55 columns: `OUTPUT_SCHEMA.names` (10) + `CALENDAR_VARIABLE_COLUMNS` (44) + `holidays_retrieved_at_utc` (1)); new `load_calendar(path) -> pd.DataFrame` schema-validating reader; new `assemble_calendar(cfg, *, cache="offline") -> Path` orchestrator that composes NESO demand + weather + holidays caches into a Stage 5 feature-table parquet. The weather-only schema is preserved as an exact prefix of the calendar schema (positions 0..9), so downstream code that reads only the weather columns composes with either frame by column-name selection.
- Stage 5: `bristol_ml.train._resolve_feature_set(cfg) -> tuple[FeatureSetConfig, LoadFn, tuple[str, ...]]` — resolves the active Hydra group-swap (`features=weather_only` or `features=weather_calendar`) to the persisted `FeatureSetConfig`, the assembler loader (`load` vs `load_calendar`), and the resolved feature column names. Both-or-neither populated raises `ValueError`. The CLI now feeds the resolved columns into both `LinearConfig.feature_columns` (unless the user pinned them via Hydra) and `harness.evaluate(..., feature_columns=...)` / `compare_on_holdout(..., feature_columns=...)` so the linear regressor, evaluator, and three-way benchmark never disagree on "what are the features?". The CLI's `metadata.name` substitutes `linear-ols-weather-only` / `linear-ols-weather-calendar` via a `_NamedLinearModel` wrapper — `LinearModel` itself stays unchanged.
- Stage 5: Hydra config groups `conf/ingestion/holidays.yaml` (three-division defaults, gov.uk URL, OGL v3 attribution) and `conf/features/weather_calendar.yaml` (cache filename `weather_calendar.parquet`, inherits demand aggregation and forward-fill semantics from the shared `FeatureSetConfig`). `conf/config.yaml` defaults list re-wired from package-override form to group-swap form (`- features: weather_only` replacing `- features/weather_only@features.weather_only`) so `features=weather_calendar` at the CLI swaps which feature file loads. New Pydantic schemas in `conf/_schemas.py`: `HolidaysIngestionConfig`; `IngestionGroup.holidays`; `FeaturesGroup.weather_calendar`.
- Stage 5: `notebooks/05_calendar_features.ipynb` — thin demo notebook. Runs the same `LinearModel` class against both feature sets with the same `SplitterConfig`, displays the 49-coefficient `results.summary()` as the teaching-moment table (facilitators read the GB load profile off the hour-of-day coefficients), prints the per-fold metric tables side-by-side (MAPE drops from ~17% to ~5% on the notebook's narrowed splitter; R² rises from 0.21 to 0.92), runs the three-way `compare_on_holdout` vs NESO when the forecast cache is warm, and plots residuals on a shared test week so the weekly ripple that weather-only misses and calendar features flatten is visible (AC-5). Runs end-to-end in ~10 s on warm caches, well inside the 120 s D8 budget.
- Stage 5: spec-derived test suite — holidays ingestion tests (VCR-backed cassette replay, all three `CachePolicy` paths, schema enforcement, duplicate-key rejection, `min_inter_request_seconds` default), calendar-derivation tests (column inventory, `is_weekend` guard, pre-window WARNING, `hour_of_day` reads UTC, DST-day row counts), assembler tests (`CALENDAR_OUTPUT_SCHEMA` prefix invariant, `assemble_calendar` idempotence, cross-schema `load` rejection, provenance-scalar preservation), `train` CLI tests (`_resolve_feature_set` four-way decision table, `features=weather_calendar` in-process smoke, regression on the Stage 4 `weather_only` path). All green.
- Stage 5: retrospective at [`docs/lld/stages/05-calendar-features.md`](docs/lld/stages/05-calendar-features.md); [`docs/architecture/layers/features.md`](docs/architecture/layers/features.md) module inventory extended to cover `calendar.py` and the assembler's second schema, with the "feature-table schema contract for Stage 5" and "population-weighting home" open questions marked resolved (H-4).

### Changed

- Stage 12 (**breaking**): all six model families (`NaiveModel`, `LinearModel`, `SarimaxModel`, `ScipyParametricModel`, `NnMlpModel`, `NnTemporalModel`) migrated from `joblib` to `skops.io` as the canonical save / load primitive. Artefact filename in the registry flips from `model.joblib` to `model.skops`. **Existing `data/registry/` directories are invalidated — `registry.load` rejects any run directory still carrying `model.joblib` with a clear `RuntimeError`; users must retrain.** Rationale: the serving layer is a network-facing deserialiser; `joblib.load` on an attacker-controlled artefact is RCE. The human's explicit direction at Ctrl+G: *"Include skops. This includes a network facing interface so security should be paramount, as I don't want an RCE exploit on my PC."* `linear` and `sarimax` use the envelope-of-bytes pattern (`statsmodels results.save(BytesIO)` wrapped in a skops-safe dict); the four other families use native skops with registered custom classes. `save_joblib` / `load_joblib` in `bristol_ml.models.io` are retained with `DeprecationWarning` for one stage and will be removed at Stage 13.
- Stage 12: `docs/architecture/layers/registry.md` — "Stage 12 inflection point" section updated to record that skops was adopted (not deferred); on-disk layout diagram updated to show `model.skops`; dispatch table extended with `nn_temporal`; status line updated; cross-reference to `serving.md` added.
- Stage 12: `src/bristol_ml/registry/CLAUDE.md` — cross-reference to the serving layer doc and Stage 12 retro added.

- Stage 8: `src/bristol_ml/evaluation/harness.py::_build_model_from_config` (standalone at `harness.py:475`) and `src/bristol_ml/train.py::_cli_main` (inline dispatcher at lines 218-262) both gained a fourth branch dispatching `ScipyParametricConfig → ScipyParametricModel` (plan D10). Local `from bristol_ml.models.scipy_parametric import ScipyParametricModel` imports keep the harness module lazy with respect to scipy. `_target_column` helper in both files extended to recognise `ScipyParametricConfig`. The train CLI's `ScipyParametricConfig` branch uses an information-only log line when `feature_columns` is pre-set — a reminder that the field constrains *Fourier* columns, not raw inputs (plan D2 clarification). The dispatcher-duplication ADR question carries forward as H-4 / B1 for Stage 11 or a dedicated housekeeping stage (plan D11: re-deferred one more time).
- Stage 8: `src/bristol_ml/models/__init__.py` lazy re-export surface extended with `ScipyParametricModel` and `ScipyParametricConfig` (the latter re-exported from `conf._schemas` for ergonomic notebook use). Import cost remains lazy — `bristol_ml.models` does not import scipy at package-load time.

- Stage 7: `src/bristol_ml/evaluation/harness.py::_build_model_from_config` (standalone at `harness.py:475-491`) and `src/bristol_ml/train.py::_cli_main` (inline dispatcher at lines 218-252) both dispatch `SarimaxConfig → SarimaxModel`. The dual-site pattern is a consequence of Stage 4's Hydra-discriminated-union design; missing either is a silent exit-code-3 bug (plan surprise 3). Local `from bristol_ml.models.sarimax import SarimaxModel` imports keep the harness module lazy with respect to statsmodels.
- Stage 7: `src/bristol_ml/models/__init__.py` lazy re-export surface extended with `SarimaxModel` and `SarimaxConfig` (the latter re-exported from `conf._schemas` for ergonomic notebook use). Import cost remains lazy — `bristol_ml.models` does not import statsmodels at package-load time.
- Stage 7: housekeeping H-2 from the Stage 7 plan — corrected the "Next" cell in [`docs/lld/stages/06-enhanced-evaluation.md`](docs/lld/stages/06-enhanced-evaluation.md) from the drafted "explicit daily (`s=24`) and weekly (`s=168`) seasonal orders" to the DHR framing that actually shipped (daily `s=24` inside SARIMAX; weekly period absorbed via Fourier exogenous regressors).

- Stage 6: `notebooks/04_linear_baseline.ipynb` — appended Stage 6 diagnostic-plot appendix (cells 13–18); Stage 4 Cells 0–12 preserved verbatim (plan D11, human mandate 2026-04-20).
- Stage 6: default matplotlib figsize for `bristol_ml.evaluation.plots` helpers is 12×8 (meetup-projector legibility); overridable via `PlotsConfig.figsize`.
- Stage 6: `src/bristol_ml/evaluation/CLAUDE.md` extended with the `plots` surface and the D9 API-growth trigger note; `src/bristol_ml/evaluation/__init__.py` lazy re-exports extended with the seven `plots` helpers plus `OKABE_ITO` / `SEQUENTIAL_CMAP` / `DIVERGING_CMAP`.
- Stage 6: housekeeping H-3 from the Stage 5 retrospective — corrected the "Next" cell in [`docs/lld/stages/05-calendar-features.md`](docs/lld/stages/05-calendar-features.md) from "Stage 6 tree-based models" (copy-paste residue) to "Stage 6 enhanced evaluation & visualisation".

### Fixed

- Stage 6 (Phase 3 review response): `bristol_ml.evaluation.plots.forecast_overlay` silently drew the second prediction line in the same colour as the Actual series. The palette slice `OKABE_ITO[1:] + OKABE_ITO[3:]` included `OKABE_ITO[2]` (sky blue, the Actual colour) at position 1; corrected to `OKABE_ITO[1:2] + OKABE_ITO[3:]`. Notebook Cell 15 (which overlays Naive / Linear / NESO) now renders all four series in distinct colours. Regression test added. (Phase 3 review B1.)
- Stage 6 (Phase 3 review response): `bristol_ml.evaluation.plots.forecast_overlay_with_band` silently drew an inverted band when the caller passed a reversed `quantiles=(high, low)` tuple. Positional `iloc[:, 0]` / `iloc[:, 1]` access on the unstacked groupby-quantile took whichever column order pandas preserved from the input list, producing a negative-height `fill_between`. Now rejects any `quantiles` that is not a strictly ordered `(lo, hi)` pair within `[0.0, 1.0]` with a `ValueError`; label-based column access added as defence-in-depth. Regression tests added. (Phase 3 review N1.)

- Stage 5: `python -m bristol_ml.train` now supports `features=weather_calendar` as a Hydra group override; the weather-only path remains the default and is unchanged behaviourally. The printed per-fold header names the active feature set so scrolling two terminal sessions side-by-side remains readable.
- Stage 5: `conf/config.yaml` `defaults:` list flipped from package-override form (`- features/weather_only@features.weather_only`) to group-swap form (`- features: weather_only`). Runtime effect: `cfg.features.weather_only` and `cfg.features.weather_calendar` are mutually exclusive — exactly one is non-`None` per run. Stage 3 / Stage 4 callers that only ever swapped `conf/features/weather_only.yaml` values (not the group) are unaffected.

- Stage 4: `bristol_ml.models` — new models layer. Public surface: the `@runtime_checkable` `typing.Protocol` `Model` (five members `fit`, `predict`, `save`, `load`, `metadata`) and the frozen `ModelMetadata` provenance record (name, `feature_columns`, `fit_utc`, `git_sha`, free-form `hyperparameters`); joblib-backed atomic IO helpers `save_joblib(obj, path)` / `load_joblib(path)` with a `skops.io` upgrade note for Stage 9+; concrete `bristol_ml.models.naive.NaiveModel` (seasonal-naive baseline, three `strategy` variants with `same_hour_last_week` default per D1) and `bristol_ml.models.linear.LinearModel` (statsmodels OLS per D2 with automatic `sm.add_constant` when `fit_intercept=True`). Module CLIs `python -m bristol_ml.models.naive --help` and `python -m bristol_ml.models.linear --help`. Full layer contract at [`docs/architecture/layers/models.md`](docs/architecture/layers/models.md).
- Stage 4: `bristol_ml.evaluation.metrics` — four pure `(y_true, y_pred) -> float` point-forecast metrics (`mae`, `mape`, `rmse`, `wape`) as fractions (DESIGN §5.3), plus a `METRIC_REGISTRY` mapping lowercase name → callable. MAPE raises on any zero in `y_true` (D8); WAPE uses `Σ|y_true − y_pred| / Σ|y_true|` (Kolassa & Schütz 2007 / Hyndman) and raises on `Σ|y_true| == 0` (D9). Array-likes (`np.ndarray`, `pd.Series`, `list[float]`) are accepted and coerced at a shared `_coerce_and_validate` gate that rejects length mismatches and NaN. Module CLI `python -m bristol_ml.evaluation.metrics --help`.
- Stage 4: `bristol_ml.evaluation.harness` — `evaluate(model, df, splitter_cfg, metrics, *, target_column="nd_mw", feature_columns=None) -> pd.DataFrame` fold-level evaluator that iterates `rolling_origin_split_from_config`, calls `model.fit` / `model.predict` per fold, and returns a long-form per-fold DataFrame (`fold_index`, `train_end`, `test_start`, `test_end`, plus one float column per metric). Defaults `feature_columns` to every weather column in `assembler.WEATHER_VARIABLE_COLUMNS` so the harness and `LinearModel` cannot disagree on "what are the features?". One structured loguru INFO line per fold plus a completion summary with total folds, elapsed time, and per-metric mean ± std.
- Stage 4: `bristol_ml.evaluation.benchmarks` — `align_half_hourly_to_hourly(df, *, aggregation="mean", value_columns=...)` D4 aggregator (HH → hourly, `mean` default, `first` alternative) and `compare_on_holdout(models, df, neso_forecast, splitter_cfg, metrics, *, aggregation="mean", target_column="nd_mw", feature_columns=None) -> pd.DataFrame` three-way benchmark helper. Runs each candidate through `harness.evaluate` and scores the aligned NESO series against its own outturn on the intersection of fold test-periods ∩ forecast coverage; returns a DataFrame indexed `[*sorted(models), "neso"]` with one column per metric. Empty intersections raise rather than returning an empty frame. Module CLI `python -m bristol_ml.evaluation.benchmarks --help`.
- Stage 4: `bristol_ml.ingestion.neso_forecast` — NESO Day-Ahead Half-Hourly Demand Forecast Performance ingestion. Public interface `fetch(config, *, cache=CachePolicy.AUTO) -> Path` and `load(path) -> pd.DataFrame` targeting CKAN resource `08e41551-80f8-4e28-a416-ea473a695db9` (April 2021+). Writes half-hourly parquet to `${BRISTOL_ML_CACHE_DIR:-data/raw/neso_forecast}/neso_forecast.parquet`; on-disk schema (`timestamp_utc`, `settlement_date`, `settlement_period`, `demand_forecast_mw`, `demand_outturn_mw`, `ape_pct`, `retrieved_at_utc`) documented in [`src/bristol_ml/ingestion/CLAUDE.md`](src/bristol_ml/ingestion/CLAUDE.md). Module CLI `python -m bristol_ml.ingestion.neso_forecast [--cache auto|refresh|offline]`.
- Stage 4: `bristol_ml.train` — end-to-end train + evaluate CLI (`python -m bristol_ml.train [overrides ...]`) wiring config resolution, `assembler.load`, model instantiation by Hydra group (`model=naive` / `model=linear`), `harness.evaluate`, and optional three-way `benchmarks.compare_on_holdout` into a single invocation. Exit codes `0` (success), `2` (required config group or cache missing), `3` (model variant has no harness factory).
- Stage 4: Hydra config groups `conf/model/naive.yaml`, `conf/model/linear.yaml`, `conf/ingestion/neso_forecast.yaml`, `conf/evaluation/metrics.yaml`, and `conf/evaluation/benchmark.yaml`. `conf/config.yaml` defaults list gains the four new entries; `model: linear` is the default per intent "Demo moment". New Pydantic schemas in `conf/_schemas.py`: `ModelMetadata`, `NaiveConfig`, `LinearConfig` (discriminated union with `Field(discriminator="type")` on `AppConfig.model`), `MetricsConfig`, `NesoBenchmarkConfig`, `NesoForecastIngestionConfig`.
- Stage 4: `notebooks/04_linear_baseline.ipynb` — thin demo notebook. Fits both models, prints the `ModelMetadata` for the naive fit and `results.summary()` for the linear fit, runs `harness.evaluate` on both with a weekly-stride override for notebook pacing (per D7), runs the three-way `compare_on_holdout` when the NESO forecast cache is warm (else prints a skip note), and plots the linear residuals plus a 48-hour overlay of actuals + every available forecast. Runs end-to-end in ~8 s on warm caches, well inside the 120 s D7 budget.
- Stage 4: spec-derived test suite — 13 benchmarks tests (alignment contract + three-way table shape/identity), 17 harness tests (H-1 UTC guard, per-fold semantics, loguru summary line), 20+ metrics tests (hand-computed fixtures, D8/D9 zero-denominator policies, length/NaN rejection), 24 linear + 19 naive model tests (protocol conformance, save/load identity, statsmodels coefficient recovery, re-entrant `fit`), 6 `train` CLI tests (in-process `_cli_main` + one `subprocess.run` smoke, with/without NESO forecast cache), 9 NESO-forecast ingestion tests (VCR-backed cassette replay, schema enforcement, provenance scalar), plus config tests for the discriminated-union model swap. All green.
- Stage 4: retrospective at [`docs/lld/stages/04-linear-baseline.md`](docs/lld/stages/04-linear-baseline.md); new layer doc [`docs/architecture/layers/models.md`](docs/architecture/layers/models.md); evaluation layer doc extended to cover metrics + harness + benchmarks; ADR 0003 records the `typing.Protocol` choice for the `Model` interface (D3).

- Stage 3: `bristol_ml.features.assembler` — feature-table assembler joining hourly-aggregated NESO demand with the population-weighted national weather aggregate. Public interface `OUTPUT_SCHEMA: pa.Schema`, `build(demand_hourly, weather_national, config, *, neso_retrieved_at_utc=None, weather_retrieved_at_utc=None) -> pd.DataFrame`, `load(path) -> pd.DataFrame`, `assemble(cfg, cache="offline") -> Path`. Writes atomic parquet to `${BRISTOL_ML_CACHE_DIR:-data/features}/<feature_set>.parquet` via `ingestion._common._atomic_write`. Emits one structured loguru INFO line per `build()` call covering demand-NaN rows dropped, per-variable forward-fill counts, and weather-NaN rows dropped after the fill cap. Module CLI `python -m bristol_ml.features.assembler [--cache auto|refresh|offline] [overrides ...]`. On-disk schema (ten columns: `timestamp_utc`, `nd_mw`, `tsd_mw`, five weather variables, two provenance columns) documented in [`src/bristol_ml/features/CLAUDE.md`](src/bristol_ml/features/CLAUDE.md).
- Stage 3: `bristol_ml.evaluation` — new layer introduced to host time-series cross-validation primitives. First member `bristol_ml.evaluation.splitter.rolling_origin_split(n_rows, *, min_train, test_len, step, gap=0, fixed_window=False) -> Iterator[tuple[np.ndarray, np.ndarray]]` plus a `rolling_origin_split_from_config(n_rows, SplitterConfig)` sugar wrapper. Expanding-window default; `fixed_window=True` slides a constant-size training window. Module CLI `python -m bristol_ml.evaluation.splitter [--n-rows N] [overrides ...]`.
- Stage 3: Hydra config groups `conf/features/weather_only.yaml` (feature-set name, demand aggregation `mean` or `max` per `Literal`, cache location, forward-fill cap) and `conf/evaluation/rolling_origin.yaml` (one-year training window, 24-h day-ahead horizon, expanding window default). Wired through `AppConfig.features: FeaturesGroup` and `AppConfig.evaluation: EvaluationGroup` via four new Pydantic models in `conf/_schemas.py`.
- Stage 3: `notebooks/03_feature_assembler.ipynb` — thin demo notebook that primes the Stage 1 and Stage 2 caches, assembles the `weather_only` feature table, round-trips through `assembler.load`, enumerates rolling-origin folds, and plots fold boundaries overlaid on the GB demand series (year-long view plus a seven-fold zoom). Runs end-to-end in ~6 s on warm caches.
- Stage 3: spec-derived test suite — 17 assembler tests (determinism modulo provenance, column-order and dtype conformance, clock-change autumn / spring, missing-data policy including the D5 structured log), 52 splitter tests (no train/test overlap, fold-count arithmetic, gap handling, expanding vs fixed window, all four parameter boundaries, infeasibility surface), 7 implementer-derived CLI tests (atomic persistence, idempotence, missing-config refusal). `tests/conftest.py` gains a loguru-to-caplog adapter fixture so INFO-line assertions pass without plumbing changes in production code.
- Stage 3: retrospective at [`docs/lld/stages/03-feature-assembler.md`](docs/lld/stages/03-feature-assembler.md); layer docs [`docs/architecture/layers/features.md`](docs/architecture/layers/features.md) and [`docs/architecture/layers/evaluation.md`](docs/architecture/layers/evaluation.md); architecture-index layers table extended.

- Stage 2: `bristol_ml.ingestion.weather` — Open-Meteo historical-weather archive ingestion. Public interface `fetch(config, *, cache=CachePolicy.AUTO) -> Path` and `load(path) -> pd.DataFrame`; writes long-form hourly parquet (one row per station × hour) to `${BRISTOL_ML_CACHE_DIR:-data/raw/weather}/weather.parquet`. On-disk schema (`timestamp_utc`, `station`, five weather variables, `retrieved_at_utc`) documented in [`src/bristol_ml/ingestion/CLAUDE.md`](src/bristol_ml/ingestion/CLAUDE.md).
- Stage 2: `bristol_ml.ingestion._common` — shared helpers extracted from `neso.py` (`CachePolicy`, `CacheMissingError`, `_atomic_write`, `_cache_path`, `_respect_rate_limit`, `_retrying_get`, `_RetryableStatusError`). Retry / rate-limit / cache-path helpers accept any config satisfying the structural `Protocol` types (`RetryConfig`, `RateLimitConfig`, `CachePathConfig`) — zero-behaviour-change refactor for Stage 1.
- Stage 2: `bristol_ml.features` — features layer introduced one stage earlier than DESIGN §9 implies so the Stage 2 notebook can import a weighted-mean helper instead of reimplementing it. `bristol_ml.features.weather.national_aggregate(df, weights)` collapses long-form per-station hourly weather into a wide-form national signal; subset-of-stations and NaN-safe weight renormalisation documented in the module docstring.
- Stage 2: Hydra config group `conf/ingestion/weather.yaml` wired through `AppConfig.ingestion.weather` (`WeatherStation`, `WeatherIngestionConfig` in `conf/_schemas.py`). Ten UK population centres with ONS 2011 Census BUA weights; default variables `temperature_2m`, `dew_point_2m`, `wind_speed_10m`, `cloud_cover`, `shortwave_radiation`.
- Stage 2: module CLIs `python -m bristol_ml.ingestion.weather` and `python -m bristol_ml.features.weather` — standalone Hydra-driven entry points per principle §2.1.1.
- Stage 2: `notebooks/02_weather_demand.ipynb` — thin demo notebook that primes both caches, joins hourly demand to the national weather aggregate, and plots temperature vs demand with a LOWESS fit; narrative motivates Open-Meteo over Met Office DataHub (AC5) and frames the curve as a hockey-stick rather than a symmetric V.
- Stage 2: recorded HTTP cassette `tests/fixtures/weather/cassettes/weather_2023_01.yaml` (London + Bristol, January 2023, five variables, ~64 kB) plus the one-off recorder at `scripts/record_weather_cassette.py`; `statsmodels` added as a runtime dependency for LOWESS.

- Stage 1: `bristol_ml.ingestion.neso` — NESO historic demand ingestion. Public interface `fetch(config, *, cache=CachePolicy.AUTO) -> Path` and `load(path) -> pd.DataFrame`, with `CachePolicy` values `AUTO | REFRESH | OFFLINE` and a `CacheMissingError` raised when `OFFLINE` is requested without a cache. Writes half-hourly parquet to `${BRISTOL_ML_CACHE_DIR:-data/raw/neso}/neso_demand.parquet`; on-disk schema (canonical `timestamp_utc` in UTC, plus `timestamp_local`, `settlement_date`, `settlement_period`, `nd_mw`, `tsd_mw`, `source_year`, `retrieved_at_utc`) documented in [`src/bristol_ml/ingestion/CLAUDE.md`](src/bristol_ml/ingestion/CLAUDE.md).
- Stage 1: Hydra config group `conf/ingestion/neso.yaml` wired through `AppConfig.ingestion.neso` (`IngestionGroup` + `NesoIngestionConfig` in `conf/_schemas.py`). Covers the NESO annual resources for 2018–2025; adding a year is one YAML list entry.
- Stage 1: module CLI `python -m bristol_ml.ingestion.neso [--cache auto|refresh|offline] [overrides ...]` — calls `fetch` with the composed Hydra config and prints the resulting cache path.
- Stage 1: `notebooks/01_neso_demand.ipynb` — thin demo notebook that loads the cached parquet via `neso.load`, resamples to hourly, and plots one representative week of `nd_mw` plus daily peaks across the cached window.
- Stage 1: recorded HTTP cassette `tests/fixtures/neso/cassettes/neso_2023_refresh.yaml` (two paginator pages of 500 rows for resource `bf5ab335-9b40-4ea4-b93a-ab4af7bce003`) plus the one-off recorder at `scripts/record_neso_cassette.py`, so integration tests replay offline under `--record-mode=none`.
- Stage 1: retrospective at [`docs/lld/stages/01-neso-demand-ingestion.md`](docs/lld/stages/01-neso-demand-ingestion.md).

### Changed

- Stage 4: `bristol_ml.evaluation.__init__` lazy re-exports extended — the four metric functions and `METRIC_REGISTRY`, the `evaluate` harness, and the two benchmark helpers (`align_half_hourly_to_hourly`, `compare_on_holdout`) all resolve via `from bristol_ml.evaluation import ...` alongside the splitter. The Stage 3 carry-over **H-3** also lands here: `rolling_origin_split_from_config` joins `rolling_origin_split` in the namespace.
- Stage 4: `docs/architecture/layers/evaluation.md` extended to cover the metric functions, the evaluator harness, and the benchmark helper; the "Benchmark comparison — evaluation layer or `benchmarks` peer?" open question (D5) marked resolved with a back-reference to the Stage 4 plan. Architecture index `docs/architecture/README.md` gains a `models` row; `docs/architecture/ROADMAP.md` drops the realised Models entry.
- Stage 4: `SplitterConfig` gains one-line British-English comments above `min_train_periods`, `test_len`, `step` (Stage 3 carry-over **H-2**) so the no-default fields document their semantics without ABI churn on the `frozen=True` model.
- Stage 2: `bristol_ml.ingestion.neso` refactored to import `CachePolicy`, `CacheMissingError`, `_atomic_write`, `_cache_path`, `_respect_rate_limit`, `_retrying_get`, and `_RetryableStatusError` from `bristol_ml.ingestion._common`. Public interface unchanged; Stage 1's 35 tests continue to pass without modification.
- Reorganised `docs/` into a tiered `intent/` / `architecture/` / `lld/` layout to support `.claude/hooks/tiered-write-paths.sh` from the lead agent. `DESIGN.md` moved to `docs/intent/DESIGN.md`; ADRs to `docs/architecture/decisions/`; stage retrospectives to `docs/lld/stages/`. Lead agent frontmatter wired to the tiered hook.

### Fixed

- Stage 4 (**H-1** carry-over): `bristol_ml.evaluation.harness.evaluate` now raises `ValueError` when the input DataFrame's index is tz-aware but not UTC — closes the Stage 3 Phase 3 review carry-over that left `rolling_origin_split_from_config` tz-agnostic. Tz-naive indices remain permitted (splitter semantics unchanged); Europe/London (or any other non-UTC tz) raises at the harness boundary before any fold is enumerated.

## [0.0.0] — 2026-04-18

### Added

- Project scaffold: `pyproject.toml` (hatchling, Python 3.12), `.gitignore`, `README.md`, `CLAUDE.md`.
- Hydra + Pydantic config pipeline: `conf/config.yaml`, `conf/_schemas.py`, `src/bristol_ml/config.py`, `src/bristol_ml/cli.py`.
- `python -m bristol_ml` entry point via `src/bristol_ml/__main__.py`.
- `py.typed` marker.
- Test suite (`tests/unit/test_config.py`) covering config load, `extra="forbid"` rejection, and the `--help` smoke.
- CI workflow (`.github/workflows/ci.yml`) — ruff format check, ruff lint, pytest via uv.
- Pre-commit hooks (`.pre-commit-config.yaml`).
- Docs: `docs/architecture.md` (pointer), ADRs 0001 and 0002, `docs/stages/00-foundation.md`.
